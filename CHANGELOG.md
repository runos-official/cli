# Changelog

## v1.15.0

**`runos shell`** opens a shell in your own workspace on a cluster, the same pod the console's
terminal shows. `runos shell -- kubectl get nodes` runs one thing and exits.

THE CONNECTION GOES STRAIGHT TO THE CLUSTER. The RunOS API is asked for the key and the workspace's
address and nothing else; the bytes go from your machine to the cluster's own front door on 443.
That is deliberate, because a terminal is judged on latency and a relay in the middle adds a hop to
every keystroke. It also means it needs the cluster's own address to be reachable from where you
are, which is exactly what the console's terminal needs.

It opens the `devops` shell, which carries kubectl, k9s and stern. The workspace is created on
first use if you have never opened one, and the first one on a cluster pulls a large image, so it
waits up to ten minutes and says what it is waiting for.

A one-shot returns the command's own exit code, so it can be used in a script. The session carries
no exit code of its own, so the shell prints it and the CLI takes that line off the output. The one
exception is a command that ends the shell itself, such as a bare `exit`, which returns 0.

WHAT YOU TYPE AFTER `--` IS WHAT RUNS, argument for argument, the same as `kubectl exec --`. Your
quoting is kept, so `-- grep "error 500" app.log` searches for the whole phrase. Pipes, redirects
and semicolons are shell syntax rather than arguments, so ask for a shell when you want them:
`-- bash -lc 'a | b'`.


RunOS accounts are now a CLI-wide local context. The new account commands manage known accounts without retaining inactive credentials.

Every account addition or switch uses browser authentication. An account mismatch stops the change before RunOS stores the new credential.

The VPN daemon now keeps a stable device identity for each account. An account switch preserves other identities.

`runos status --json` reports a CLI and VPN account mismatch when synchronization needs recovery.

RunOS Desktop can now be installed and managed through the CLI on macOS:

```
runos desktop install
runos desktop status --json
runos desktop update
runos desktop uninstall
```

The installer verifies the checksum, GitHub provenance, repository, workflow, tag, and application bundle. The installer replaces the application atomically.

`runos update` updates the CLI first. The command then updates Desktop when Desktop is installed. JSON results report both components separately.

## v1.14.0

`runos vpn` connects this machine to your RunOS clusters over a WireGuard-compatible tunnel, with no config file to manage. Each machine is a device with its own key (generated on the machine, never uploaded) and its own address. A sign-in lasts 24 hours; after that the tunnel is cut and `runos vpn up` signs you in again in the browser.

```
sudo runos vpn install     # once: installs the background service (needs admin)
runos vpn up               # sign in and connect to your default cluster
runos vpn connect <cid>    # reach another cluster
runos vpn status           # the tunnel and each cluster
runos vpn down             # disconnect and end the session
```

The client asks conductor what it should reach and converges to it: one WireGuard interface, one peer per connected cluster, routes for each cluster's overlay range, and split DNS so a cluster's names resolve over the tunnel while everything else resolves normally. A cluster with no VPN server, or one you are not connected to, is shown as such rather than hidden.

A tunnel is a person's session, so a personal access token cannot bring it up; `runos vpn up` runs the browser sign-in.

The service runs as a launchd daemon on macOS, a systemd unit on Linux (split DNS through systemd-resolved) and a Windows service (split DNS through NRPT rules); the Windows archive ships `wintun.dll` beside `runos.exe`, and `runos update` keeps the two together. When an operator moves a cluster's VPN server to another node (`runos services wireguard <id> move`), a connected client follows the new endpoint on its next poll without a command.

Requires conductor with manifest 38.12.0 or newer (39.1.0 for `services wireguard move`).

## v1.13.0

`runos vms ssh <vmid>` opens an interactive SSH session on a virtual machine, or runs one command on it. RunOS never gives a VM a public address, so this tunnels to the guest's SSH port through the RunOS API and works from anywhere the API is reachable, with nothing configured on the guest and no VPN.

The key RunOS manages for that VM is fetched for the session, written to a private temporary file that is deleted when the session ends, and never added to your agent or your SSH config.

The tunnel is an ssh `ProxyCommand` rather than a local listening port, which means there is no port to collide, nothing left behind if the process dies, and no window where a port is open to everything else on the machine. It also means `scp`, `sftp` and `rsync` work over the same route with no extra machinery:

```
scp -o ProxyCommand="runos vms proxy myvm" file runos-admin@vm:/tmp/
```

Host key checking is off for these sessions on purpose: every VM answers as the same placeholder host through its own proxy, so recording keys would file one VM's under the next VM's name. The transport is already authenticated end to end by a single-use session ticket and the platform's own mTLS to the cluster.

Requires conductor with manifest 35.20.0 or newer.

A failed API call now prints what the server said, not the envelope it arrived in. Before:

```
Error: API error (404): {"error":"VM nope1 not found on this cluster.","code":"vm.not_found","traceId":"a136..."}
```

After:

```
Error: VM nope1 not found on this cluster. (HTTP 404, vm.not_found)
```

The message, the status and the machine-readable code survive; the trace id stays in the body and in `--json`, where it is useful, rather than on every error line. A body that is not the expected JSON (a proxy's HTML error page, say) is still printed in full, because it is the only evidence there is.

## v1.12.2

`apps_sync` no longer 400s when it removes secret/env keys. Sync is a declarative full replace computed from your local files, so a key the server has but your local set lacks is an intentional deletion (already shown in the sync plan's Remove list). Conductor's partial-drop guard (live since conductor 1.9.0) was rejecting those legitimate removals; the CLI now sends `allowDrop` on the full-replace secret/env update so declared deletions apply, while the guard still catches an accidental partial `set`.

Install via `https://get.runos.com/cli.sh?release=v1.12.2`.

## v1.12.1

Richer `runos status` output. When signed in, status now shows the account's company name and website (from the account profile) and resolves the default cluster's display name, rendering `Default Cluster: <name> (<cid>)`. If the configured default cid no longer exists on the account (e.g. after an account switch), status flags it as `(not found on this account)` instead of silently printing a stale id. The Environment line is removed from the human-readable output (users are always on prod, so it was noise); `--json` still includes `environment` and gains `companyName`, `website`, `defaultClusterName`, and `defaultClusterMissing`. All enrichment calls are best-effort with a 10s timeout, so `runos status` keeps working offline.

Install via `https://get.runos.com/cli.sh?release=v1.12.1`.

## v1.12.0

Lowers the MCP read server's documentation gate from 3 topics to 2. After `mcp_bootstrap`, other read tools now unlock once the LLM has read 2 distinct topics (platform-overview plus one task topic is sufficient context; the bootstrap's topic router pulls further reads in via `see also:` links). Pairs with the slimmer conductor bootstrap payload (manifest 32.0.0, `topicKeys` instead of the full topic index) to cut per-session token overhead for MCP-driven assistants.

Install via `https://get.runos.com/cli.sh?release=v1.12.0`.

## v1.11.3

Fixes `runos apps pull` / `runos apps sync` mishandling a port mapping whose `standardHttps` field is absent on the server (legacy app docs). The platform default is public routing (`standardHttps: true`), but the CLI zero-valued the missing field to `false`, so a pull materialized `standardHttps: false` in the local yaml and the next deploy persisted it, silently moving the app off standard HTTPS (443) onto the VPN-only entrypoint. `Port.StandardHttps` is now a pointer with the default resolved everywhere it is read (pull write-out, sync wire payload, drift compare), so absence means `true` and only an explicit `false` opts a port out of public routing. Conductor >= 1.5.2 also always sends the field explicitly; this fix keeps older conductors safe too. Regression tests cover pull materialization, drift compare in both directions, and the sync payload.

Install via `https://get.runos.com/cli.sh?release=v1.11.3`.

## v1.11.2

Fixes a deploy regression introduced by v1.11.1's "fail loud on a missing explicit env file" change: the *second* deploy of an app could fail on its own env file. On first deploy the CLI writes the canonical default `secretEnv:` / `env:` filenames back into `runos.yaml`. On the next deploy those written-back values looked like operator-authored explicit references, so the missing-file guard fired and aborted, even though the default secret file is legitimately absent when an app has no secret vars. A configured value that equals the canonical auto-derived default is now treated as the CLI's own round-trip (non-explicit) and stays exempt from the fail-loud check; a genuinely custom filename keeps the v1.11.1 protection. Regression tests cover the round-trip default, custom-name, and missing-file cases.

Install via `https://get.runos.com/cli.sh?release=v1.11.2`.

## v1.11.1

Bug fixes and licensing.

- **`runos deploy` / `runos apps sync` now fail loud when an explicit `env:` or `secretEnv:` file is missing.** A `runos.yaml` naming a non-existent env file previously deployed EMPTY env vars with no error, e.g. silently wiping `ALLOWED_CIDRS` and disabling an app's in-app source-IP allowlist. Both paths now hard-error on an explicit-but-missing reference (`apps sync` is replace-all, so a typo'd path would have deleted all server-side env vars). Auto-derived default env files stay exempt (legitimately absent on first deploy). Regression tests cover explicit-missing, default-missing, present, and empty cases.
- **`runos nodes delete --follow` no longer errors on a successful synchronous delete.** A plain delete completes synchronously with no job; `--follow` now only waits when the response carries a real `jobId`, instead of failing with "response does not contain jobId" on a delete that actually succeeded.
- **Licensing.** The repo now carries an explicit LICENSE (Elastic License 2.0, source-available) + NOTICE and a README License section; internal AI-assistant files (`.claude/`, `CLAUDE.md`) are no longer tracked, matching the agent repos. No CLI behavior change.

Install via `https://get.runos.com/cli.sh?release=v1.11.1`.

## v1.11.0

Friendlier first run. A fresh install (no `~/.runos`) previously printed two alarming blocks before `runos login` even opened the browser: `Unable to load manifest: config not found ...` and `Note: failed to fetch CLI manifest on first run ...`. Both were expected pre-login states dressed up as failures, with recovery jargon aimed at the wrong audience. The CLI now distinguishes "not signed in yet" from "set up but broken":

- **Welcome on bare `runos`.** A not-signed-in user gets a short welcome pointing at `runos login`, instead of help-text noise or a manifest error. Signed-in users still get the normal help output.
- **Pre-login warnings suppressed.** The manifest "first run" note no longer fires when the only reason the fetch failed is that you have not authenticated yet. The loud `runos manifest update` / recovery diagnostic is now reserved for the genuine case it was written for: signed in, but the manifest still failed to load.
- **Login nudge instead of silence.** A signed-out user who runs a manifest-driven command (e.g. `runos services list`) gets a one-line `Run 'runos login' to get started.` rather than a silent non-zero exit.
- **Manifest warmed after login.** Both interactive and `--api-key` login now best-effort fetch the manifest on success, so dynamic commands work on the very next invocation with no manual `runos manifest update`.

Internally this adds an `auth.ErrNotAuthenticated` sentinel (so the not-authenticated state is detected via `errors.Is`, not string matching) and a network-free `auth.HasCredentials` check. No change to authentication, transport, or any command's behavior once signed in.

Install via `https://get.runos.com/cli.sh?release=v1.11.0`.

## v1.10.1

Restores self-update for old CLI binaries. Releases v1.0.0-v1.6.0 hardcode a `runos-latest-{os}-{arch}` asset name in their updater's download and checksum-lookup paths; the v1.7.0 asset rename to `runos-{os}-{arch}` left those binaries 404ing on every `runos update`. Each release now also publishes a byte-identical `runos-latest-{os}-{arch}` copy (listed in `checksums.txt`, attested like the canonical asset), so a stuck old binary can self-update to current. No CLI behavior change; release-pipeline only. The alias copies are a temporary bridge to be removed once no pre-1.7.0 clients remain.

Install via `https://get.runos.com/cli.sh?release=v1.10.1`.

## v1.10.0

Node affinity round-trip for apps, plus two related fixes:

- **`nodeAffinityTags` in runos.yaml.** Pin an app's pods to nodes carrying RunOS tags (a required nodeAffinity on the Deployment). Entries are tag names: `key` for value-less tags, `key:value` for composite tags (`runos tags list` to discover; the server resolves by exact match and rejects unknown names rather than auto-creating tags). The field round-trips through `apps pull`, drift-detects in `apps diff`, and pushes via `apps sync` and `runos deploy`. Desired-state semantics: omitting the field (or `[]`) clears the pin on the next deploy/sync, same family as `healthCheck` / `deploymentStrategy`. Multiple tags must ALL be on one node (AND, not alternatives). Requires conductor manifest 30.x.
- **`deploymentStrategy` no longer clobbered by sync.** The field was missing from `apps sync`'s drift detection and wire body entirely, so a yaml carrying it never pushed the preset, and a yaml without it silently cleared a console-set preset with no plan line. It now drift-detects and pushes like the other desired-state fields.
- **Inline `envVars:` / `secretEnvVars:` in runos.yaml are rejected** by `apps sync` / `apps diff` with the same fix-it hint `runos deploy` emits (plain env vars live in the `env:` side file; secrets push via `apps env-vars set`). Previously the typed decode silently dropped them, so the vars never reached the container and nothing said so.
- **Deploy prints conductor advisory warnings.** `runos deploy` now surfaces the prepare response's `warnings` array on stderr, currently used when a `nodeAffinityTags` pin matches zero nodes (pods would sit Pending/Unschedulable until a node is tagged). The deploy proceeds; the warning closes the feedback gap.

Install via `https://get.runos.com/cli.sh?release=v1.10.0`.

## v1.9.0

Gates the PostgreSQL teardown verbs `runos services postgresql drop-user` and `drop-database` behind the uniform destructive-command guard: on a TTY they now prompt `y/N` before running, and in a non-TTY (CI, piped) they refuse unless `--yes`/`-y` is passed. This matches the existing protection on `delete-*` / `revoke-*` / `remove-*` verbs, so an irreversible role/database drop can no longer fire from a stray invocation. The match is on the `drop-` prefix and is method-agnostic, so any future `drop-*` verb inherits the same guard automatically.

No other user-facing change. This release also pins the BuildKit `set-advanced-configs` manifest surface (CLI flags + MCP write tool) with regression tests; that command is fully manifest-driven, so there is no CLI behavior change, the tests guard the field-name -> flag-name mapping against conductor manifest drift.

Install via `https://get.runos.com/cli.sh?release=v1.9.0`.

## v1.8.0

Adds the `runos services postgresql clone-database` command: copy a managed PostgreSQL database into another instance, cross-cluster or same-cluster, within one account. The copied database lands on the target owned by the source's role with its username AND password preserved (read from the source's managed-user secret, not regenerated), so an app still holding the old connection string keeps working after the migration.

```
runos services postgresql clone-database <target-instance-osid> \
  --source-cid <cid> --source-osid <pgOsid> --source-db <dbName> \
  --target-db <dbName> --owner <role> --follow
```

The target cluster is the ambient `--cid` (or `runos config set cid` default), like every other `services postgresql` verb; only `--source-cid` carries a cluster, since the source may live elsewhere. `--follow` streams the clone job to completion and exits non-zero if it fails.

The command is manifest-driven; this release adds the CLI-side flag presentation so the discrete flags render as `--source-osid` / `--source-db` / `--target-db` while the request body keeps the conductor's wire field names. Requires a conductor that serves the `clone-database` endpoint. Install via `https://get.runos.com/cli.sh?release=v1.8.0`.

## v1.7.5

Fixes three stored-PAT / `RUNOS_API_KEY` auth defects found together; all three repro on a PAT-only config (`api_key` set, no Firebase session):

- **`runos mcp` was unusable under a PAT (blocking).** The MCP auth gate hard-required a Firebase refresh-token, so `mcp_bootstrap` and every manifest-driven tool failed with "authentication required" even though the same PAT worked for ordinary CLI commands. `getAuthToken` now routes through `auth.ResolveToken` (`RUNOS_API_KEY` -> stored PAT -> Firebase), matching the rest of the CLI.
- **`runos status` falsely reported "Not logged in" under a valid PAT.** The auth check only honoured the Firebase fields, so a PAT user saw a misleading unauthenticated status while every command succeeded. Status now recognises a stored PAT or `RUNOS_API_KEY` as authenticated and shows the credential type (`(PAT)` / `(PAT via RUNOS_API_KEY)`), with an `authMethod` field in `--json`.
- **`runos login --api-key` could store a PAT against the wrong tenant.** When `--account-id` was omitted it fell back to whatever account was already in config; since a PAT is account-scoped, a new PAT minted for account B run while config held account A was stored against A (authenticated-but-wrong-tenant, worse than a clean failure). `--account-id` is now required with `--api-key`; the stale-config fallback is gone. The CI env path (`RUNOS_API_KEY` + `RUNOS_ACCOUNT_ID`) is unaffected.

Install via `https://get.runos.com/cli.sh?release=v1.7.5`.

## v1.7.4

Fixes a login bug where a stored PAT silently shadowed a fresh interactive login. After `runos login --api-key <pat>` against one environment, switching environments and running an interactive `runos login` (browser or `preauth` device flow) reported "Authenticated successfully!" but left the old PAT in `~/.runos/config.json`. Since `ResolveToken` ranks a stored PAT above the Firebase session, every subsequent call sent the stale key and failed with `401 Invalid token`. Both non-PAT login paths now clear the stored PAT on success, so the fresh session takes over (the mirror of `--api-key` login clearing the refresh token). Workaround on older versions: `runos logout`, or remove the `api_key` field from the config. Install via `https://get.runos.com/cli.sh?release=v1.7.4`.

## v1.7.3

Maintenance release. No user-facing CLI behavior change from v1.7.2. Cut as the phantom patch for obj-55 (installer signature-level verification): publishing it produces a fresh build-provenance-attested release archive so the updated `get.runos.com/cli.sh` installer can fetch and `gh attestation verify` a current artifact end to end. The release process itself re-downloads the published asset and verifies its attestation is bound to `release.yml @ refs/tags/v1.7.3`, exercising the download+verify path from the producer side at the same time. Install via `https://get.runos.com/cli.sh?release=v1.7.3`.

## v1.7.2

Maintenance release. No user-facing CLI behavior change from v1.7.1. Cut as the supervised verification target for the obj-52 re-rollout: publishing it drives a fresh, observed release event through the live validation pipeline (release webhook to HMAC verify to build-provenance attestation verify to the shared R2 digest registry to the `current.json` flip), so the fail-closed install guarantee can be exercised end to end under supervision across dev, beta, and prod. Install via `https://get.runos.com/cli.sh?release=v1.7.2`.

## v1.7.1

Maintenance release. Adds the deterministic release runbook used to cut and verify CLI releases, and is the first release to flow through the live build-provenance validation pipeline (release webhook to attestation verify to the R2 digest registry). No user-facing CLI behavior change from v1.7.0. Install via `https://get.runos.com/cli.sh?release=v1.7.1`.

### Internal

- **Release runbook** (`scripts/release.sh`, `make release`, and the `release-cli` skill): one deterministic, fail-closed path for cutting a release. Gates (`go build`/`go vet`/`go test`/`make local`), fast-forward `main` to `dev`, tag, push, watch CI, and verify the published asset's attestation, plus a public-repo sensitivity scan over the release payload. Serves as the entry point for automated / foreman-dispatched releases.

## v1.7.0

Hardens CLI distribution: every released archive now carries a keyless build-provenance attestation, and the archives are renamed to `runos-{os}-{arch}` so a downloaded binary can be verified against a server-published digest before it runs. Install via `https://get.runos.com/cli.sh?release=v1.7.0`.

### New

- **Build-provenance attestation on every release** (foreman obj-50). The release workflow now runs `actions/attest-build-provenance` (keyless, Sigstore/OIDC, no long-lived secret), attesting each platform archive to the workflow's OIDC identity `https://github.com/runos-official/cli/.github/workflows/release.yml`. This lets the distribution side prove a downloaded `runos` is the exact artifact this pipeline built, defending against post-build tampering (replace-asset / swap-checksums) of the released archives.
- **Release trust model documented** (`SECURITY.md`). Captures the full chain (keyless attestation, server-side validator + digest registry, fail-closed installers) and the accepted limitation that attestation does not defend against a compromised build, plus the admin-only GitHub controls (branch protection, required review, restricted workflow edits, restricted tag/release publishing) that are the only mitigation for that limitation. Every `uses:` in `release.yml` and `ci.yml` is SHA-pinned.

### Changed (one-time break)

- **Release archives renamed from `runos-latest-{os}-{arch}` to `runos-{os}-{arch}`** (`.tar.gz` for darwin/linux, `.zip` for windows). The literal `latest` is dropped so asset names align 1:1 with the digest-registry path. This is a deliberate one-time break: existing `releases/latest/download/runos-latest-*` URLs stop resolving for releases cut from this version on; consumers must move to exact-tag download URLs. `checksums.txt` tracks the new names automatically.
- **Self-updater synced to the new asset names** (`internal/update`). `runos update` now downloads the renamed asset and matches the corresponding `checksums.txt` line. Note: a `runos` binary from v1.6.0 or earlier still looks for the old `runos-latest-*` name, so updating onto v1.7.0 from an older binary requires a fresh install via the installer rather than `runos update`.

## v1.6.0

Adds `runos login --api-key`, a way to persist a personal access token (PAT) as the active credential instead of the interactive browser flow. Install via `https://get.runos.com/cli.sh?release=v1.6.0`.

### New

- **`runos login --api-key <pat> [--account-id <id>]`**: stores a PAT in `~/.runos/config.json` (already `0600`) and switches the active credential to it, so every subsequent command authenticates with the PAT without exporting `RUNOS_API_KEY` in each shell. Previously a PAT was only consumable via the env var; this is the "force a PAT on this machine" path for a developer who wants headless-style auth locally.
  - **Precedence is unchanged where it matters**: the `RUNOS_API_KEY` environment variable still wins over a stored key, so a CI runner overrides a local default. Order is env var > stored PAT > Firebase refresh-token. A stored PAT short-circuits before any Firebase exchange.
  - **Account id**: `--account-id` is stored alongside the key; if omitted, the account already in config (from a prior login) is kept. A PAT addresses a specific account, so a login with neither is refused rather than silently account-less. The id is charset-validated (alphanumeric, 1-64) before it can be joined into request paths.
  - **The token is never echoed** to the terminal on store, and `runos logout` now clears the stored PAT alongside the Firebase credentials (URLs and default cluster are preserved). Setting a PAT clears any existing Firebase refresh-token state so exactly one credential is active.
  - **No MCP surface**: setting a PAT stays CLI-only. The MCP server continues to inherit `RUNOS_API_KEY` from its host environment and never accepts a raw token as a tool argument, so the secret can't leak into tool output / LLM context.

## v1.5.0

Adds `runos services harbor build-image`, a first-class way to build an auxiliary (non-app) container image from a local build context and push it into the cluster's system Harbor under the managed `runos-apps` project. Install via `https://get.runos.com/cli.sh?release=v1.5.0`.

### New

- **`runos services harbor build-image`**: build an arbitrary image that is NOT tied to an app's own Dockerfile (sidecar images, shared base images, one-off tooling images) and push it to `runos-apps/<repo>:<tag>` using Harbor's managed push credentials. Decoupled from apps, deployments, and VCS: no app id, no commit SHA, no robot/RBAC/GitHub cred, and no phantom build-only app cluttering `apps list`.
  - **Local-context upload.** `--context <dir>` is tarballed (Docker-standard `.dockerignore` semantics, keeping dotfiles and `runos.*.yaml` that a generic Dockerfile may COPY) and uploaded directly to the cluster agent, which builds it on the same in-cluster BuildKit that builds app images. `--dockerfile` (default `Dockerfile`) is relative to `--context` and validated to stay inside it before any upload.
  - **Fixed project, multi-tag.** The project is fixed to `runos-apps` (not a flag); `--repo` is the repository name within it. Pass `--tag` one or more times: the same built image is pushed to each, and mutable tags (e.g. `latest`) are overwritten silently like a normal docker push.
  - **Fire-and-forget by default, `--follow` to wait.** Without `--follow` the command prints the `jobId` plus a `runos follow` hint and exits 0 the moment the conductor accepts the request. With `--follow` it streams build logs and exits non-zero on build failure so a CI step gates on the outcome. `--json` carries a structured envelope (`jobId`, `repo`, `tags`, `images`, and `skippedBecauseCached`/`durationMs` once `--follow` reaches a terminal state).
  - **`--build-arg KEY=VALUE` parity with deploy** (repeatable): parsed and validated by `internal/buildargs`; duplicate keys within a single invocation are rejected.
  - **MCP**: exposed as the `services_harbor_build-image` tool so agents can trigger the same build-and-push from a local context (the build context is a local path, so it's a hand-written shim, not a manifest-generated tool).
  - **Security**: the server-supplied upload URL is scheme-validated (an absolute URL must be https) and redirects are disabled before the bearer token is attached, so the upload token can't be downgraded to plaintext or bounced to another host.

## v1.4.2

Follow-ups to v1.4.1: surfaces `--app` in `--help` and stops `--command` mangling bareword literals. Install via `https://get.runos.com/cli.sh?release=v1.4.2`.

### Fixes

- **`--app` is now visible in `--help` on app-scoped commands** (foreman #82). v1.4.1 added `--app` as a normalizer redirect to `--id`, but the redirect was invisible in `--help`, so the alias was undiscoverable. It's now a real cobra flag on app-scoped commands (`Application ID (alias for --id; same value)`). Passing both `--id` and `--app` with different values is a pre-network "pass one or the other" error, and the missing-arg hint mentions both flags.
- **`--command` no longer coerces bareword literals to the wrong JSON type** (foreman #83). `runos apps run --command true` (or `false` / `0` / `1234` / `null`) hit a conductor 400 (`command[0] must be a non-empty string (got boolean)`) because dynacmd JSON-decoded every element of a string-array flag. The array coercer now consults the field's manifest `itemType`: `string`-typed arrays (the `--command` case) pass through verbatim, so numbers, JSON keywords, and JSON-shaped fragments all survive as literal strings on the wire; object/array item types keep their JSON-unwrap behaviour.

## v1.4.1

Lets app-scoped manifest commands accept `--app` as an alias for `--id`, matching the hand-coded `deploy` / `apps build` verbs. Install via `https://get.runos.com/cli.sh?release=v1.4.1`.

### Fixes

- **`--app` accepted as an alias for `--id` on app-scoped manifest commands** (foreman #80). `runos deploy` and `runos apps build` both take `--app <id>`, but manifest-driven app commands (`apps run`, `apps show`, `apps logs`, ...) only registered `--id`, so `runos apps run --app <id>` failed with `unknown flag: --app` — a copy-paste trap for CI scripts following the build → run → deploy pattern. The dynacmd flag-normalizer now redirects `--app` to `--id` on app-scoped commands (endpoint contains `/apps/:id` and the primary positional is named `id`). The alias stays off services commands (e.g. `services postgresql show`, where `id` is the service id), so a stray `--app` there still surfaces `unknown flag`. Invisible in `--help`, same precedent as the existing camelCase→kebab and primary-id-alias plumbing.

## v1.4.0

Adds `runos apps build`, a standalone image-build verb that builds and pushes a VCS app's SHA-keyed image to Harbor without rolling it out. Install via `https://get.runos.com/cli.sh?release=v1.4.0`.

### New

- **`runos apps build`**: builds and pushes a VCS app's image for a given SHA and stops, with no rollout and no command run. Optional and opt-in for clean CI logs; the build-on-demand short-circuit inside `runos run` / `runos deploy` is unchanged, so the canonical CI shape (`apps build` then `run` then `deploy` on the same SHA) reuses the one image already in Harbor.
  - **Two invocation shapes, copied from `runos run`.** CI: `runos apps build --app <id> --sha <sha> --cid <cid> [--follow]` (no yaml on disk). Laptop: `runos apps build [--follow]` (id from `runos.yaml`, `--sha` defaults to git HEAD with a dirty-tree refusal unless `--allow-dirty`, `--cid` via the same three-source order as deploy).
  - **VCS-only.** A CLI-deploy app is refused with the same shaped error `runos run` emits, after a server-side `deployType` lookup and before any build POST.
  - **Fire-and-forget by default, `--follow` to wait.** Without `--follow` the command prints the `jobId` plus a `runos follow` hint and exits 0 the moment the conductor accepts the request. With `--follow` it streams cluster-agent build progress and exits non-zero on build failure so a CI step gates on the build outcome.
  - **`--build-arg KEY=VALUE` parity with deploy** (repeatable): parsed and validated by `internal/buildargs` and forwarded as `buildArgsCli` in the `POST /apps/:id/build` body, so the SHA-keyed image is byte-identical to one a subsequent `deploy` / `run` builds at the same SHA and args. Argless builds keep the wire body identical to today's deploy/run bodies.
  - **`--json` envelope on stdout** (progress routes to stderr): `{ jobId, appId, sha, configPath, imageTag?, skippedBecauseCached?, durationMs? }`. The optional fields populate only when `--follow` reaches a terminal state and the conductor's structured job result is present.
  - **MCP**: a new `apps_build` tool, forwarding `app`, `sha`, `cid`, and `build_arg`.

### Fixes

- **MCP empty-body 2xx responses now emit a valid text content block** (foreman #78). `ContentBlock.Text` carried `json:"text,omitempty"`, so an empty-text success frame marshalled to `{"type":"text"}` with no `text` field, which MCP rejects as an `invalid_union` (conformant clients saw a Zod validation dump even though the call succeeded, e.g. `account api-keys revoke` returning 200 with an empty body). Two-part fix in the shared wrapper so every no-content endpoint behaves at once: drop `omitempty` so empty frames marshal to `{"type":"text","text":""}`, and detect empty/whitespace-only bodies after a 2xx to return a deterministic `"<command> succeeded (<status>)"` string instead of failing `json.Unmarshal` on empty bytes.

## v1.3.0

`runos services postgresql adopt-user` joins the Postgres-service verbs. Take an existing Postgres role under RunOS management in one shot, with the credentials then injectable into an app via `requires:` like any greenfield user. Install via `https://get.runos.com/cli.sh?release=v1.3.0`.

### New

- **`runos services postgresql adopt-user`**: sibling to `create-database`. Where create-database is greenfield, adopt-user takes over an existing role (restored, cloned, or migrated). One verb does it all: ensure role + database exist, reassign object ownership in the target database to the role (tables + sequences + `public` schema; extensions stay owned by `postgres`), set a RunOS-managed password, and register as a managed user so `services postgresql user-credentials` returns the creds and `requires:` injects them into apps. Idempotent. Requires exactly one of `--rotate-password` (generate a managed value) or `--password <specific>` (operator-supplied), so RunOS always ends up holding the password and `requires:` injection always has a complete credential. The verb is manifest-driven and picked up automatically by `runos manifest update`.
- **Discrete-field `requires.env` injection**: an app's `requires.<alias>.env` map can route individual Postgres credential fields to arbitrary env-var names, not just the legacy single `url: DATABASE_URL` shape. Supported field keys: `url`, `host`, `port`, `username`, `password`, `database`. `user` is also accepted as a back-compat alias for `username`; new yamls should prefer `username` for parity with the adopt-user verb's `--username` flag. No yaml schema change — the existing `map[string]string` already fits. The drift-gate / collision-detection helpers in `internal/apps/requires_env.go` already iterate field-key-agnostically; regression-test coverage in `internal/apps/requires_env_test.go` pins both vocabularies so a future refactor can't accidentally tighten the iteration to a legacy `url`-only shape or drop the `user` alias.

## v1.2.0

Adds `runos run`, a new top-level verb for executing a one-off task in the cluster against a VCS app's image. Install via `https://get.runos.com/cli.sh?release=v1.2.0`.

### New

- **`runos run`**: sibling to `runos deploy`. Where deploy is build then rollout, run is build then execute. The typical use is pre-rollout work (DB migrations, seeds, backfills) that today still depends on `kubectl exec` and CI-level cluster credentials. The canonical CI shape is two ordered steps, `runos run ... scripts/release.sh` then `runos deploy ... --sha <same>`: both verbs are keyed on the commit SHA so the second one reuses the image already in Harbor and only does the rollout.
  - **Two invocation shapes.** CI: `runos run --app <id> --sha <sha> --cid <cid> [--] <script-or-command>` (no yaml on disk). Laptop: `runos run [--] <script-or-command>` (loads `runos.yaml` from cwd, defaults `--sha` to git HEAD with a dirty-tree refusal unless `--allow-dirty`, and resolves `--cid` via the same three-source order as deploy).
  - **VCS-only.** CLI-deploy (tarball) apps are refused locally before any conductor call; they have no meaningful commit SHA for the build-on-demand short-circuit.
  - **`--timeout` flag.** Accepts a Go duration (e.g. `30m`, `1h`). The conductor enforces a 7200s hard cap and a 1800s default when omitted. A timeout kill exits non-zero.
  - **Exit-code propagation.** The CLI exits with the container's real exit code: a non-zero command exit becomes a non-zero CLI exit so a CI step gating on the run will fail when the command fails. The job result's `exitCode` field is the source of truth.
  - **`--json` flag.** Mirrors deploy's contract: the streamed progress routes to stderr, and stdout carries a single JSON envelope (`jobId`, `osid`, `appId`, `sha`, `status`, `exitCode`) at the end for `jq`-driven CI gates.
  - **MCP**: a new `run` tool is registered under the `sensitive_write` category. Accepts `app`, `sha`, `cid`, `command` (array), and `timeout`. The subprocess blocks until terminal state and returns the streamed output plus exit code in one text payload; a 60-minute outer ceiling fires past the conductor's hard cap.

## v1.1.0

Adds Docker build-time arguments to `runos deploy`. Install via `https://get.runos.com/cli.sh?release=v1.1.0`.

### New

- **Docker build args on `runos deploy`**: apps that bake build-time configuration into the image (e.g. a Next.js bundle that needs `NEXT_PUBLIC_*` at build time) can now pass values without bespoke GitHub Actions workarounds. Two sources, merged server-side:
  - **`buildArgs:` in the app yaml** — a `KEY: VALUE` map that round-trips through `apps pull` / `apps sync` (the field mirrors onto `PulledApp` when the server surfaces it).
  - **`--build-arg KEY=VALUE` on the CLI** (repeatable) — parsed and validated before any HTTP call. ARG names must match Docker's `^[A-Za-z_][A-Za-z0-9_]*$`, duplicate keys are rejected, and commas in values survive (the flag is a `StringArray`). The parsed list rides on both the `prepare-cli-deployment` body (CLI deploy) and the `POST /apps/:id/deploy` body (VCS deploy) under a separate `buildArgsCli` field. The conductor merges yaml `buildArgs:` with `buildArgsCli` server-side; the CLI never pre-merges. Argless deploys omit both fields for back-compat.
  - **MCP**: the `deploy` tool gains a `build_arg` array property, forwarded as one `--build-arg` per entry to the `runos` subprocess.

## v1.0.0

General-availability release. Promotes the v1.0.0-rc.8 feature set to stable; see the rc.8 entry below for the full Apps/Services-as-IaC, VCS deploy, PAT auth, MCP, and hardening story. Install via `https://get.runos.com/cli.sh?release=v1.0.0`.

### Changes since rc.8

- **Thin-yaml pulls on a named RRC**: `apps pull` no longer emits `replicas` / `cpuRequestMc` / `cpuLimitMc` / `memoryRequestMb` / `memoryLimitMb` alongside `resourceRequirementClassId: app.sl1.beff` (or any other named class). The class already bakes those dimensions in, so carrying them in the yaml was pure noise and produced spurious "intent vs resolved state" ambiguity on re-read. `custom` rrcId continues to emit every field explicitly.
- **Client-side flip-to-custom on conflicting overrides**: when a local yaml carries a named RRC and a `replicas` / cpu / memory value that disagrees with the class defaults, `apps sync` now detects the conflict before the wire body is built. It flips `resourceRequirementClassId` to `custom`, backfills the missing resource fields from the server snapshot, and surfaces a one-line notice in the sync plan so the promotion is visible rather than silent. The conductor's resolveRRC behaviour is unchanged; the CLI just makes the outcome explicit in the diff and the wire payload.
- **`deploymentStrategy` round-trips through pull and deploy**: a yaml that sets `deploymentStrategy: recreate` (the right pick when the next pod can't schedule until the old one releases a single GPU, an RWO PVC, or an exclusive port) now actually reaches the deploy wire body. Pre-fix the field was silently dropped and the conductor's `rolling` default applied regardless. `apps pull` mirrors the same field so the value survives a pull/edit/deploy round-trip.
- **`clusters kubeconfig` text mode emits raw YAML** (foreman #48). Pre-fix the default text output included framing the kubeconfig parser couldn't consume; piping to `kubectl --kubeconfig` works without `--json` now.
- **`maintenance-scripts run` gated behind a destructive `[y/N]`** (foreman #37). Aligned with the rest of the destructive-confirm surface: the gate auto-skips when stdin is not a terminal, `--yes` is the explicit opt-out, and the prompt surfaces the script identifier so a mistyped run doesn't fire blind.
- **`-f file=value` coerces per manifest field type** (foreman #40). YAML values supplied via `-f` are now typed against the manifest's declared field schema (int → int, bool → bool, object → object) instead of being passed as bare strings, so `-f replicas=3` reaches the API as `3`, not `"3"`.

## v1.0.0-rc.8

Consolidated release candidate on top of v0.3.9. Supersedes the rc.1 through rc.7 series (those tags and releases were removed in favour of this single, cohesive entry). The CLI's IaC workflows, VCS deploy support, env-var split, regression-test scaffolding, and assorted hardening all ship in one tag. Install via `https://get.runos.com/cli.sh?release=v1.0.0-rc.8`.

### New

- **Apps as IaC**: `runos apps pull` / `apps diff` / `apps sync` round-trip an app's full configuration (yaml, env vars, secret files, manifest overrides) between cluster and disk. Pull supports bulk mode (`--all`), single-app mode, code-archive download (`--code` / `--code-version`), drift gates, and section-level diff reports. Sync pushes deltas via dedicated endpoints (PATCH /apps, env-vars, secret-files, overrides, requires).
- **Services as IaC**: `runos services pull` / `services diff` / `services sync` for postgresql, valkey, mysql, and any future service type the manifest exposes. Schema is manifest-driven (zero per-type code in the CLI); a new service type or new settable field on the API side flows through automatically after `runos manifest update`. Service yaml shape is desired-state only: `runos.service.<cid>.<type>.<sid>.yaml` contains exactly the fields the user can set; audit, derived, and operational fields the show response carries are filtered out automatically. Filename is convention; renames are tolerated via header-based lookup.
- **Apps integration with services IaC**: `apps_pull` cascades into pulling linked service yamls; `apps_sync` refuses `requires:` entries without an `id:` (provisioning is the service-yaml's job); `runos deploy` keeps the `requires.<alias>.class` shorthand and writes a service yaml on first-time provisioning.
- **PAT-based authentication**: `runos account api-keys add` / `list` / `show` / `update` / `revoke` for managing personal access tokens. The CLI consumes a PAT via `RUNOS_API_KEY` (plus `RUNOS_ACCOUNT_ID`, optional `RUNOS_API_URL`); when set, the CLI bypasses Firebase entirely. Headless CI runners need no `~/.runos/config.json` and no interactive `runos login`.
- **VCS deploys**: `runos deploy` dispatches on the app's `deployType`. CLI-deploy apps tarball the local source and upload (the classic flow). VCS-deploy apps send `{sha, configPath}` only; conductor and cluster agent pull source from the linked GitHub/GitLab integration at the SHA, build in-cluster, push, roll out. Flags: `--app <id>` for CI mode (no yaml on disk), `--sha` (defaults to `git rev-parse HEAD`), `--allow-dirty` (waives the dirty-tree refusal). The two modes never silently intermingle: passing `--sha` / `--allow-dirty` against a CLI-deploy app is a hard error, and passing `--app` against a CLI-deploy app is rejected after a server-side `deployType` lookup.
- **`configPath:` is the source of truth for a VCS yaml's repo location**: `runos deploy` auto-derives the repo-relative path of the local yaml (`git rev-parse --show-toplevel` + `filepath.Rel`) and sends it on every VCS deploy. Conductor persists it to the AppDocument, so subsequent CI deploys (`runos deploy --app <id> --sha <sha>` without a checkout) inherit the persisted value. Monorepos with apps under per-app subdirectories work without manual config. Explicit `configPath:` field in the yaml is honoured as an escape hatch.
- **Env vars split into Secret + ConfigMap**: alongside `.runos.<cid>.<id>.env` (sensitive, gitignored, K8s Secret), `runos deploy` / `apps pull` / `apps sync` / `apps diff` also handle `runos.<cid>.<id>.config.env` (plain, committed, K8s ConfigMap). Both flow into the pod via `envFrom` so app code reads them identically. A key may not appear in both files at once; conductor refuses the deploy with a 400 listing conflicts, and the CLI pre-flights the same check. Pull/sync/diff round-trip both sides independently.
- **`runos deploy --follow` streams per-step build logs**: the follow poller fetches new work-item log entries on each tick (cursor-paginated via `/jobs/:id/workitems/:id/logs`) and indents them under their parent step. GitHub Actions runs wrap streamed output in a `::group::` block and surface the public URL via `::notice::` after a successful deploy.
- **`runos follow <jobId>`**: blocking job-status follower with line-oriented output (one line per state change, no escape codes, no spinners, no repaints). Suitable for CI logs and AI consumers; exit code gates downstream pipeline steps. Exposed on MCP as `jobs_follow` (long-poll wrapper, blocks until terminal state).
- **Topic-driven MCP documentation**: the MCP read server requires the LLM to consume a minimum number of distinct topics (apps, services, api-keys, cicd, ...) before non-topic tools unlock, so AI assistants ground themselves in the workflow before executing it.
- **Service delete guard (409)**: `DELETE /services/<type>/<id>` returns a structured 409 listing dependent apps when any reference the service. The CLI renders this as a multi-line refusal naming each dependent and its alias, with reconcile guidance.
- **Empty-secret-env wipe gate**: `apps sync` refuses to push when the local secret-env file is empty (or carries only platform-injected names like `DATABASE_URL`) AND the server has user-set secret keys. Catches the fresh-checkout footgun where a gitignored secret file isn't on disk and the LLM/CI silently wipes prod secrets. The refusal names the keys that would be deleted. Pass `--allow-empty-secret-env` (CLI) / `allow_empty_secret_env=true` (MCP) when the wipe is intentional. The `--yes` confirm-skip flag does NOT waive this gate.
- **`apps pull --keep-env`**: opt-out from env file rewrites. Useful when the user has dev-only env edits they want to keep while still refreshing yaml, secret-files, and overrides. Stdout shows the kept paths so the user can run `apps diff` to see the divergence. Exposed on the MCP `apps_pull` tool as `keep_env`.
- **Pre-deploy drift gates**: `runos deploy` refuses when the local yaml has diverged from the running app on the server, and separately when newer CLI archives exist (someone deployed via console or CI between this directory's last pull/deploy and now). Both are fail-closed: an API failure refuses the deploy. Pass `--force` to override; the diff is shown either way.
- **TESTING.md**: testing philosophy and conventions. Pure-function regression tests on extracted helpers are the dominant pattern; the project explicitly does NOT write cobra-level subprocess tests or end-to-end tests against a live conductor.
- **`runos deploy` confirmation prompt**: a pre-deploy summary + `[y/N]` gate fires on both CLI and VCS deploy paths so a mistyped `runos deploy` (wrong directory, wrong cluster, wrong sha) doesn't silently kick off a build. Auto-skipped when stdin is not a terminal (CI, piped input). The `--yes` / `-y` flag is the explicit opt-out for TTY users who want the old behaviour. Summary lists app + cluster + account, plus the resolved build-context path (CLI) or short SHA + configPath (VCS).

### Improvements

- **Yaml is the source of truth for cluster id**: `apps sync` / `apps diff` / `apps pull <yaml>` / `apps pull` (auto-detect) / `apps list-previous-uploads` and `services sync` / `services diff` / `services pull <yaml>` read the cluster id from the yaml's `cid:` field. `--cid` and `RUNOS_CLUSTER_ID` become optional cross-check guards: passed values are validated against the yaml, mismatch refuses the operation. CI loops over committed yamls no longer need a default cluster set, no `--cid` per call. Commands without a local yaml to source from (`apps pull --all`, `apps pull --app-id`, `services pull --type+--service-id`) still require an explicit cluster id.
- **`apps pull` / `apps diff` correctly read `deployType`**: prior versions conflated provider name with deploy type and stored `github-arc` / `gitlab-runner` as `deployType`, producing perpetual drift on `apps diff` because the server emits `vcs`. Provider identity now flows via the `Integration` block alongside repo/branch metadata; `deployType` carries the canonical `cli` | `vcs` value with `integrationType` as a legacy fallback for older conductors.
- **Class-flap heads-up note on `apps diff`**: when the server stored `resourceRequirementClassId=custom` because cpu/memory/replicas overrides disagree with the named class's defaults, the diff surfaces a one-line note explaining the mechanic and how to round-trip cleanly.
- **MCP `deploy` tool gains VCS parameters**: `app`, `sha`, `allow_dirty`. Pre-validation stays in the CLI binary (one source of truth for the deployType branching rule) so the MCP shim only translates args.
- **MCP JSON Schema for object/array fields**: the manifest-driven tool generator emits `additionalProperties: {type: string}` for `object` fields and `items: {type: string}` for `array` fields. Strict-validating LLM clients (some Anthropic SDK tool wrappers) previously rejected the unannotated schemas. The `object` manifest type maps to JSON Schema `object` instead of being collapsed to `string`. `providerOptions` (domains_create / domains_update) drops `additionalProperties` so booleans round-trip natively.
- **MCP defensive JSON-string coercion**: when an MCP client ignores the declared field type and sends an `object` / `array` field as a JSON-encoded string, the executor decodes in-place so the wire body matches the manifest's declared type.
- **Tarball walker** unconditionally excludes RunOS-managed manifests (`runos*.yaml`, `runos.*/`, `overrides/`, dot-prefixed files) from every deploy archive, regardless of `.dockerignore`. Cross-cluster config can't bleed into a source upload.
- **`apps pull` writes a default `.dockerignore`** on first pull (skipped when present) covering the same exclusions so external Docker builders honour the same rules.
- **Multi-cluster project shape**: directory-per-app (`runos.<cid>.<id>/` subdirs with `sourceDir: ".."`) is the recommended shape for repos deploying to multiple clusters; `apps_pull` defaults to it and stamps `sourceDir` automatically.
- **CDN config schema**: `RemoteDomains` reads the canonical `api` field, falling back to the legacy `conductor` field for older payloads. Driven by an `APIURL()` resolver used everywhere the CDN environment is consumed.
- **Pre-deploy drift gate validates IDs as defence-in-depth**: app ID and cluster ID from the local yaml are charset-validated via `apps.ValidateIdentifier` before being joined into URLs, so a tampered yaml can't smuggle path components into API requests.
- **Build stamp on local builds**: `make local` stamps `dev-<utc-timestamp>` into the version string, so dev binaries are version-distinguishable in the wild. Stable releases continue to stamp the git tag.
- **`runos services sync` gains `--follow`**: was previously fire-and-forget with no opt-in to wait. Now matches the project-wide convention: prints `job: <id>` + `Follow rollout: runos follow <id>` hint by default; `--follow` (`-f`) streams the job to terminal status and exits non-zero on failure. Closes the silent-success gap that `apps sync` had pre-V12.
- **Documentation**: README and CLAUDE.md gain a deploy-verb section covering the deployType dispatch, the three cluster-id sources (flag, config, yaml), the SHA / dirty-tree gate, and the env-var Secret/ConfigMap split. CLAUDE.md links to TESTING.md from the testing section. CLAUDE.md gains a "Job-following convention" section that captures the opt-in `--follow` rule. README modernized: simplified quickstart (the default environment is auto-fetched on first run, no manual `runos config env` step), added Authentication and IaC sections, environment-variable table updated with `RUNOS_API_KEY` / `RUNOS_ACCOUNT_ID` for headless CI auth.
- **MCP integration: Roo Code target removed**. `runos mcp configure roo` no longer exists; supported targets are now Claude Code, Gemini CLI, OpenCode, and OpenAI Codex. The Roo-specific `.roo/mcp.json` writer and its handler are gone.
- **`runos deploy` prints the console URL on success**: the post-deploy summary now includes a `Console:` line linking directly to the app's `manage/<id>` page in the web console. Saves the user one tab and one mental URL stitch on every deploy.
- **`runos --version`**: top-level version flag wired (was previously only available via the `cli version` subcommand).
- **Deploy tarball excludes editor backups and the plain env file**: the upload walker drops `runos.yaml.bak` / `runos.*.yaml.backup` (created by user manual edits) and `runos.*.config.env` (per-cluster ConfigMap-backed env, supplied by the platform) so neither leaks into image builds. Mirrored in the default `.dockerignore` for users running external Docker tooling.
- **Default `.dockerignore` covers more conventional secret/junk directories**: `apps_pull`'s first-pull `.dockerignore` template now also excludes `.env` / `.env.*`, `.git/`, `.gitignore`, `secrets/` / `**/secrets/`, `**/.aws/`, `**/.ssh/`, editor backups (`*.bak`, `*~`, `.*.swp`), and common language ignores (`node_modules/`, `__pycache__/`, `.pytest_cache/`, `target/`). Preserves the previous RunOS-managed-files invariant (build context can never carry RunOS yamls / sidecars / overrides).
- **`apps sync` honours user-set secret-file `mountPath:`**: pre-fix, every secret file in the sync plan rendered with the synthetic `mountPath: /<filename>` even when the yaml's `secretFiles[*].mountPath` carried a real value. The mount path now sources from the manifest entry on adds and the server preserves the existing mount on updates.
- **`apps sync` plan now shows `requires:` drift inline**: pre-fix, the YAML diff section omitted requires-block changes even though they were on the wire (the patch sends the whole body). Plan now appends per-alias requires drift (`+`/`-` lines for type/id/config/env) so the user sees what they're confirming.
- **`apps sync` infers secret-file filename from `local:` when omitted**: a yaml entry with only `local: ./certs/web.pem` (no `filename:`) used to render as `+ (mount /)` in the plan and key the content map at the empty string (silently collapsing multiple entries). The basename is now inferred and propagated to both the manifest projection and the loaded content map.
- **`apps sync` env-conflict and platform-claimed errors render the actual filename**: pre-fix, when the yaml omitted `env:` / `secretEnv:` the conflict refusal printed an empty filename (`env-var keys appear in both  and `). Defaults are now resolved (`runos.<cid>.<id>.config.env` / `.runos.<cid>.<id>.env`) for the message.
- **`apps pull` JSON output carries a drift-resolution hint**: when one or more apps abort with local drift, the JSON summary gains a `hint` field explaining that post-deploy drift is expected (server-applied defaults like `replicas` / `resourceRequirementClassId`), not user work that would be lost. Surfaces inline to the LLM/MCP caller without forcing it to read the human-readable error stream. Also folded into the CLI-side error message.
- **`apps pull` no longer flags service yamls as ambiguous app candidates**: pre-fix, a directory containing `runos.yaml` plus a `runos.service.<cid>.<type>.<sid>.yaml` (the cascaded service yaml from class-shorthand provisioning) refused with "multiple yaml candidates" on auto-detect. The pull scanner now skips `runos.service.*` files since they're handled via `runos services` verbs.
- **`apps pull` round-trips health-check port and path independently**: pre-fix, both fields were gated on a non-empty `healthCheck` preset, so a yaml carrying `healthCheckPort: 8080` (with no `healthCheck:` key) lost the port on every pull. Each field now flows through independently, matching the conductor's omit-equals-clear desired-state semantics.
- **`apps sync --json` emits the dry-run plan as structured JSON**: opt-in via `--json` alongside `--dry-run`. CI pipelines can gate on yaml / env / secretFile / override change blocks without parsing the formatted plan output. Combining `--json` with apply (no `--dry-run`) is refused up-front since the post-apply jobIDs aren't part of the plan struct.
- **`runos deploy` writes post-deploy IaC artifacts on first deploy**: a fresh `runos deploy` (no prior `apps pull`) now writes the canonical `.dockerignore` template (idempotent: existing files are left untouched) and a placeholder `runos.<cid>.<id>.config.env` so the yaml's `env:` reference points at a real file. Pre-fix, a user who only ran `runos deploy` (the documented happy path) ended up with a yaml that referenced a non-existent file and had no `.dockerignore` to protect external `docker build` invocations from leaking RunOS-managed config into images. Errors print as warnings; the deploy itself doesn't roll back since the conductor side has already accepted the request.
- **`runos deploy --follow` rolls back the source-version sidecar on build failure**: pre-fix, every deploy wrote the new archive id to `.runos.<cid>.<id>.source-version` immediately, so a subsequent `--follow` build failure left the sidecar pointing at a UUID whose image was never produced. The next `runos deploy` (or drift gate) would treat the failure as the new baseline. The CLI now captures the prior recorded version, writes the new value, and on `--follow` failure restores the prior value (or removes the file when there was no prior baseline). Fire-and-forget deploys can't observe success/failure here so they keep the new value (still useful: `apps pull --code <uuid>` works because the archive uploaded).
- **Required positional fields on manifest-driven commands also accept `--<name>` flag form**: `runos apps status --id y2w1y` was previously rejected as `unknown flag` because the dynamic command builder only registered fields declared as non-positional. The builder now exposes every required string-typed positional as a `--<name>` flag too; the executor pulls the flag value into the body map when set; the missing-arg check honours either shape. Positional supply still wins when both are given. Error message updated to mention both forms (`missing required argument: id (or pass --id)`). `cid` is excluded from the auto-flag (every command whose endpoint contains `:cid` already gets a dedicated `--cid`).
- **Dynamic command leaves silence the usage block on runtime errors**: pre-fix, every API failure on a dynamic command (404 not-found, 409 dependents-refusal, transport errors, etc.) printed the full `--help` text after the diagnostic, drowning the actual error. Cobra `SilenceUsage: true` is now set on every leaf so the user sees the error and only the error. The error itself is still printed by cobra and propagates to the process exit code.
- **`runos apps logs` (and any log-shape array endpoint) streams one line per entry instead of breaking the table**: the manifest-driven response renderer now detects `[]{timestamp, message, ...}` arrays and prints `<timestamp> [<pod>] <message>` per line (mirroring `kubectl logs --timestamps --prefix`). The pre-fix wide-table render produced thousand-character rows that wrapped past the terminal and dwarfed every other column. `--json` keeps the full structured form for tooling that needs it.
- **Table cells summarise arrays-of-objects as `[N entries]`**: the `runos apps builds` output is the canonical case: the `logs` column carried an array of nested objects which the cell formatter inline-concatenated into ~1000-char `key=value, key=value; ...` runs that broke the table layout. Cells now render as `[3 entries]` (or `[1 entry]`) for any array-of-objects field; scalar arrays and primitives are unchanged. `--json` still exposes the full nested structure.
- **Manifest-driven commands no longer demand `cid` as a positional argument**: pre-fix, every dynamic command declared with `cid` as positional+required (most service writers, system-config endpoints) failed with a positional-missing error before the executor's flag/config-default fallback could resolve it. The builder now skips the positional-missing check for `cid` (the executor returns a single clear error if all three sources are empty) and renders the use line as `[cid]` so the help text doesn't mislead users into thinking the positional was the only path.

### Bug fixes

- **`OverrideSummary` accepts both `id` and `__docId` from the conductor's overrides response**: pre-fix, after the conductor route was updated to emit `id` (the 11b normalisation away from the Firestore-internal `__docId`), the CLI's struct still only read `__docId`. Post-normalisation responses left `OverrideSummary.ID` empty, which made `apps_diff` render `id: ""` in its overrides projection and `apps_sync` issue `DELETE .../overrides/` (trailing-empty path). The 11c/11d regression. The struct now defines a custom `UnmarshalJSON` that populates ID from whichever shape is present, so a CLI binary built against one conductor generation works against either. Regression test `TestService_ListOverrides_AcceptsPostNormalisationIDField` covers the post-normalisation shape.
- **`services diff --json` exits 2 on drift**: pre-fix, the `--json` branch returned early via `return emitJSON(diff)` before the exit-2 hook ran, so `services_diff --json` always exited 0 even when the report carried a drift status. CI gates that branched on the structured signal silently passed. The exit hook now runs after both human and JSON paths, matching `apps_diff`'s contract verbatim. The MCP wrapper still translates exit 2 into a successful drift-report payload.
- **`apps sync --redact-secrets` covers plain env too**: pre-fix, the redact flag only redacted secret-side env values; a third-party API key accidentally committed to the plain `config.env` (technically disallowed but easy to do by mistake) leaked verbatim into the plan output and from there into LLM context. The flag's stated contract is "keep secrets out of LLM context", so plain env values are now redacted under the same flag. Cost: a slightly less informative plan when the user opts in. Benefit: the LLM driving the sync never sees a value it doesn't strictly need.
- **`runos deploy` code-drift gate fails closed when the recorded source version isn't on the server**: pre-fix, the gate silently skipped (and printed a warning) when the local `.source-version` referenced an archive id that was no longer in the server's archive list. That made the gate trivially bypassable: hand-edit `.source-version` to garbage and the gate goes quiet. The fix refuses the deploy unless `--force` is passed, and renders a multi-line guide with `runos apps list-previous-uploads` / `runos apps pull --code --force` reconcile commands. With `--force`, the prior warn-and-proceed behaviour is preserved.
- **Plain-side platform-claimed env vars refused on `runos deploy` and `apps sync`**: pre-fix, a `DATABASE_URL=...` (or any name claimed by a `requires.<alias>.env` mapping) committed to the plain `runos.<cid>.<id>.config.env` file was silently ignored at runtime (the requires-merge wins) but happily persisted into Git history. The CLI now refuses on the deploy and sync paths with the offending names and their claiming aliases listed; the user moves them to the secret-side file (where the platform writes its own value on every deploy and the merge re-runs harmlessly). The secret-side equivalent stays unrefused since apps_pull legitimately writes those values there and the merge re-runs on every deploy.
- **`runos config get` no longer shadowed by a duplicate dynamic `config get`**: pre-fix, the manifest's system-config namespace and the static CLI-config command both rendered as top-level `config` entries in `runos --help`, and the merged tree attached two `get` leaves to the same parent (cobra picked one nondeterministically). The dynamic command builder now skips a child whose name is already present on the merged parent, and `cmd/root.go` registers `configCmd` and `deployCmd` as existing so the merge happens at the right node. Fixes both the duplicated `--help` listing and the dispatch ambiguity.
- **`runos cli version-check` no longer reports a phantom update**: pre-fix, the bare CLI invocation (without `--version` / `--os`) sent empty strings to the manifest endpoint, which produced a misleading `updateAvailable: true` plus an alarming sentinel in `releaseNotes`. The dynacmd executor now auto-injects the running binary's version (`version.Version`) and `runtime.GOOS` when those fields are unset, mirroring the MCP wrapper. Manual overrides via `--version` / `--os` still win.
- **`services sync` CREATE plan no longer wipes non-secret values from plan output**: pre-fix, `--redact-secrets` (always set on the MCP path) replaced every value with `<redacted>` indiscriminately, so the user was asked to confirm `name: <redacted>`, `resourceRequirementClassId: <redacted>`, `storageMb: <redacted>`. Redaction is now scoped to keys whose names look sensitive (password, token, secret, apiKey, credentials, privateKey). Service config fields stay legible. Plan title also renders `<new>` for CREATE plans instead of leaving the id slot blank.
- **`services sync` CREATE no longer poisons the local yaml's `id:` field with the OSID**: pre-fix, the conductor's create response carries the long-form `osid` (e.g. `minio-v2k5k`) and the CLI fell back to it verbatim when `id` was missing on the response. The yaml's `id:` field expects the bare 5-char id, so subsequent diff/pull/sync 404'd ("Service not found") until the user fixed it by hand. The fallback now strips the `<type>-` prefix when present so the yaml stays consistent.
- **`services sync` CREATE accepts add-endpoint flags (e.g. `exposeS3On443`)**: pre-fix, `AddInputFieldNames` only walked the manifest's fields list and missed boolean flags, so valid create-time options were spuriously refused as "not patchable on this service type". Flags are now folded into the same allow-list (the dynacmd executor splits them on the wire). Refusal wording on CREATE also no longer says "patchable" since there's no service yet to patch; it now says "not accepted by this service type's add endpoint".
- **`apps diff` / `apps sync` honour default env paths when the yaml omits the field** (V3): pre-fix, an app whose yaml had no `env:` (or `secretEnv:`) field but had a file at the documented default path (`runos.<cid>.<id>.config.env` / `.runos.<cid>.<id>.env`) was silently reported `in_sync` and the file's contents never made it to the server on a sync. `BuildDiffReport` now computes the env diff sections whenever EITHER side has content (server vars OR a local file at the resolved path), and `LoadLocalEnv` falls back to the caller-supplied default when the yaml ref is empty. Symmetric with what `apps pull` writes by default.
- **Job-following convention standardised across all commands**: every command that produces a server-side job defaults to fire-and-forget; pass `--follow` (`-f` where free) to wait for terminal status. No auto-follow based on TTY, CI env vars, or any implicit signal. Applies to `runos deploy`, `runos apps sync`, `runos services sync`, and every manifest-driven command whose response shape declares a `jobId` (auto-wired in `internal/dynacmd/builder.go`). Without `--follow`, the command prints the job id and a `Follow rollout: runos follow <id>` hint. With `--follow`, it streams progress via the shared follow path and exits non-zero on terminal `failed`. Documented in `CLAUDE.md` under "Job-following convention" so the rule is canonical.
- **CLI manifest bootstrap on first run** (V6): pre-fix, fresh CI installs hit an inconsistency: `runos deploy` (statically defined) soft-warned about the missing manifest and continued, but `runos services sync` / `runos apps pull` (manifest-driven) hard-failed with "Error: load manifest...". Same root cause, different blocking behaviour. `cmd/root.go`'s `PersistentPreRunE` now runs a manifest fetch in the same shape as the existing `config.InitFromRemote` first-run bootstrap. Skip-list: `version` / `help` / `config` / `update` / `manifest *` (avoids surprising network calls and double-fetch on `runos manifest update`).
- **`apps pull` auto-updates server-side `configPath` on mismatch** (V2 / V14): pre-fix, pulling a VCS-deploy app to the canonical `runos.<cid>.<id>.yaml` naming with no awareness of where the AppDocument's `configPath` said the yaml lives would silently set up the next VCS deploy to fail at the cluster-agent's yaml-fetch step. Pull now PATCHes the server's `configPath` to match the new local layout (skipping CLI-deploy apps, and skipping when the local path can't be repo-relative-resolved because we're not in a git checkout). The action is reported on the JSON pull response as `configPathUpdated: {from, to}` so the MCP wrapper carries the structured signal through automatically. Falls back to the legacy stderr warning on PATCH failure. `--no-configpath-update` opts out for the rare case the user wants pull to leave server state alone.
- **`sourceDir` and `dockerfile` round-trip through the AppDocument** (V13): pre-fix, the local yaml's `sourceDir` (build context) and `dockerfile` (Dockerfile path within sourceDir) never reached the server, so a fresh checkout that ran `apps pull` got an incomplete yaml: every monorepo app had to manually re-add these fields after every clone or filename rename, and a missing `sourceDir` made the next build die at `failed to read dockerfile: open Dockerfile: no such file or directory`. `BuildPulledApp` now extracts both fields from the API response and writes them back via `apps pull`; `computeYAMLPatch` / `buildFullYAMLBody` push them on `apps sync`; `apps diff` reports drift on either field like other partial-update fields. Both fields apply to CLI and VCS deploys and round-trip uniformly. Edits limited to `configPath` / `sourceDir` / `dockerfile` (any combination) hit the conductor's sync fast-path (no redeploy). Local yaml values still win over server values on pull so manual edits survive re-pulls. Conductor counterpart in this release: `patchAppBuildMetadata` + `validateBuildMetadata` (lexical path containment).
- **`apps pull` recomputes sourceDir client-side during configPath auto-update** (V19): pre-fix, V14/V17's auto-update hook PATCHed `{configPath: <new>}` alone, and conductor's V13/V16 lexical containment validator merged with the stored `sourceDir`. When the new configPath dir was shallower than the old one, the merge often escaped the repo root (canonical case: relocating from `infra/runos/apps/runos.yaml` to `tmp/runos.yaml` while sourceDir stayed `../../../apps/frontend` → merged path `../../apps/frontend` → 400). V18 surfaced the failure structurally; V19 prevents it. The CLI now pre-computes a sourceDir relative to the new configPath dir that preserves the absolute repo target the existing pair pointed at, applies it to `serverState.SourceDir` BEFORE the local yaml write so the on-disk yaml is coherent at the new layout, and includes the same value in the PATCH so server and local stay in sync. Pure helper `apps.RelocateSourceDir` returns "" when no recompute is needed (no relocation, default sourceDir, naive merge stays in repo) or when the existing pair already escapes (legacy bad state); the V18 error-surface stays as the fallback in that case. `dockerfile` doesn't need touching: it's path-relative to `sourceDir`, and the preserved absolute sourceDir target keeps the dockerfile resolution chain consistent. Briefed initially as a conductor change (structured 400 with suggested replacement values); conductor team pushed back that path math belongs client-side and the half-step toward typed-error responses set the wrong API precedent. Resolved by lifting the math into the CLI with no conductor change and no API surface growth. Regression test `TestRelocateSourceDir` covers the V18 trigger, no-op cases (same-dir, default sourceDir, in-repo merge), legacy bad state, and edge inputs (10 cases).
- **`apps pull` surfaces configPath auto-update failures structurally** (V18): pre-fix, when the V14 auto-update PATCH was rejected (canonical case: conductor's V13/V16 lexical-containment validator refusing the new configPath because the stored sourceDir would escape the repo root once configPath moves to a shallower dir; also any transport / auth / 5xx error), the failure was stderr-only. MCP-driven LLMs got a pulled-app entry with no `configPathUpdated` and no error field, indistinguishable from a successful no-op pull. Fix adds a sibling `configPathUpdateError: { from, to, error }` field on the per-app entry, populated whenever `svc.UpdateApp` returns an error during the auto-update branch. The `error` field carries the conductor's response verbatim (or the transport error string), so callers see the same actionable message direct-CLI users get on stderr. Mutually exclusive with `configPathUpdated` in practice (one PATCH attempt, one outcome). MCP wrapper unchanged: the JSON passthrough at `internal/mcp/apps.go` carries the new field automatically. Deliberately did NOT add a CLI-side pre-flight that re-implements the conductor's lexical validator: the duplicate logic would silently diverge if the conductor's rules tighten or loosen, and surfacing the conductor's error verbatim gives the LLM the same actionable message anyway. Conductor stays the authoritative validator.
- **`apps pull` configPath auto-update fires regardless of CLI cwd** (V17): pre-fix, the V14 hook silently no-op'd when `apps pull` was invoked from a CWD outside the pull target's git repo, the canonical case being MCP-driven pulls where the CLI subprocess inherits the MCP host's CWD (typically the user's editor project, not `--out`). `vcsRepoRelPath` resolved git context from the process CWD via `git rev-parse --show-toplevel`, returned the wrong root (or errored), and the local-vs-server configPath comparison fell into the `localRepoRelPath == ""` skip branch. Fix anchors git resolution at the yaml's directory using new `git.IsRepoAt` / `git.RepoRootAt` helpers (`git -C <yamlDir>`), absolutizes `--out` in `resolvePullPlan` so `filepath.Dir(yamlRes.Path)` is unambiguous, and extracts the path math into a pure `apps.RepoRelPath` helper covered by `TestRepoRelPath`. CLI-deploy apps stay unaffected: `DecideConfigPathAction` short-circuits on `deployType != "vcs"` regardless of the repo-relative-path value.
- **Parallel `apps pull` no longer races for `runos.yaml`** (V1): pre-fix, fanning out N concurrent `runos apps pull --app-id <id> --out <dir>` calls (the LLM/CI shape) into a shared empty target dir would leave one outlier at the canonical `runos.yaml` and N-1 at the per-app suffixed `runos.<cid>.<id>.yaml`. Whichever pull won the race claimed the canonical slot. Fixed by routing the `id-flat` pull mode through new `apps.SaveYAMLSuffixed`, which writes straight to the suffixed leaf without consulting the directory state, so concurrent callers each land at their own deterministic path with no contention. Single-app modes (yaml-positional, bulk, id-subdir) keep using `SaveYAML` so `runos.yaml` is preserved on a re-pull where there's no race possible.
- **`apps pull` and `runos deploy` provisioning respect existing service yamls anywhere in the repo** (V4): pre-fix, projects that organise infra as `infra/runos/apps/` + `infra/runos/services/` (apps and services in sibling dirs) saw both `apps_pull`'s services cascade AND deploy's class-shorthand first-time provisioning re-create duplicate service yamls next to the app yaml because the lookup only checked the app's own directory. Both call sites now share new `services.ExistingServiceYamlPath`, which scans the entire git repo root via `FindByIDInTree` and skips the write when a matching service yaml exists anywhere reachable. Heavy dirs (`.git`, `node_modules`, `vendor`, `.runos`) are skipped during the scan to keep it cheap. Outside a git checkout the lookup falls back to a single-dir `FindByID`, preserving the single-app-at-root behaviour. `--no-services` continues to opt the cascade out entirely.
- **`apps sync` blocks per emitted job when `--follow` is set** (V12, CLI half): each step's `(job <id>)` line previously printed the moment the conductor accepted the API call, so silent cluster-side failures (`kubectl patch` on a missing configmap was the canonical case) silently completed the CLI with exit 0. With `--follow`, sync now waits for each step's terminal status, prints `FAILED: <conductor.error>` on failure, and aborts the rest of the plan with non-zero exit. Without `--follow`, sync stays fire-and-forget and prints the trailing `runos follow <id>` hints. The conductor half (`patchConfigMapData` / `patchSecretData` create-on-missing-then-patch) shipped separately.
- **`runos deploy --follow` and `runos follow <jobId>` accept `RUNOS_API_KEY`** (V11): `internal/jobs/service.go:getAuthToken` and `internal/update/updater.go:getAuthToken` were bypassing `auth.ResolveToken` and hard-requiring `cfg.Firebase`, so PAT-only CI hit `authentication required: run 'runos login' first` on the job-progress streaming endpoints (the deploy POST itself worked because it correctly used `auth.ResolveToken`). Both delegate to `auth.ResolveToken` now; the auth contract matches every other API-touching surface in the CLI.
- **`runos deploy` env merge flipped to local-wins**: the pre-deploy sync used to refuse on any same-key-different-value conflict between local and server, blocking the dominant path (every version bump, every flag flip) until the user manually reconciled. The merge now copies the local file verbatim and adds only server-only keys; the deploy that follows pushes local up and replaces the server-side env in full.
- **`runos deploy` skips env file write when the merge is a no-op**: prevents the inverse footgun where a fresh checkout with no local env file would produce a strict no-op merge, the file would never materialise, and the subsequent deploy body would carry an empty `customEnvVars` map that conductor interpreted as "user wants empty ConfigMap" and wiped server env on every deploy.
- **`apps sync` override delete now removes the orphan local file**: the delete op carries `LocalLeaf` (the leaf filename inside `appDir/overrides/` that pull would have written for this override). Apply unlinks the local file alongside the server delete; previously deleting an override server-side left the local file orphaned.
- **`apps diff` no longer flags local-only `sourceDir` as drift forever** (superseded by V13): pre-V13, the diff engine stripped top-level `sourceDir:` lines from local bytes before comparison since `sourceDir` was a CLI-only field that never reached conductor. V13 round-trips `sourceDir` and `dockerfile` through the AppDocument, so the strip is no longer needed and was removed; field-level drift on either now surfaces normally on `apps diff` and reconciles via `apps sync`.
- **`apps pull` preserves `resourceRequirementClassId: "custom"`**: prior versions rewrote the literal string `"custom"` as empty during pull, fighting with `apps show` (which surfaced the literal from the server's `resolveRRC` synthesis). Pull preserves the literal so `apps_show` and `apps_pull` return symmetric views.
- **`apps sync` no longer renders preserve-sentinels as drift**: `clusterDomainId`, `resourceRequirementClassId`, and `replicas` are partial-update fields on conductor's PATCH endpoint (omitted local value means "preserve server value", not "clear / set to zero"). Drift reporting mirrors the wire body's omit logic.
- **`apps diff` exits 2 on drift**: structured exit code so CI can branch on "config drift detected" without parsing stdout. Exit 0 = in sync; exit 2 = drift; any other non-zero = real failure.
- **MCP `apps_diff` translates exit code 2 to success**: drift is the structured signal `apps_diff` returns, not a failure mode. The MCP wrapper hands the drift report back to the LLM caller as a normal text payload. The translation lives in a pure helper (`interpretAppsCommandResult`) tested directly with synthesized `*exec.ExitError`. CI users still see the non-zero exit at the CLI layer.
- **`services sync` class-only swap no longer flips class to "custom"**: the PATCH body is drift-only (`computeDriftPatch`): fields whose local value matches the server are omitted, so a class-only swap touches only the class field and conductor's `resolveRRC` doesn't flip class to "custom" from materialised cpu/memory baselines.
- **`services pull` no longer projects class-derived cpu/memory/replica fields when a real resource class is stored**: now that the API manifest declares those fields as settable on `services/<type>/{id}/update`, the manifest-driven pull was projecting them into every yaml regardless of the stored class. The CLI mirrors the apps_pull preset-wins gate: real class → strip the five derived fields; `custom` (or empty / legacy) → keep them as the user-owned source of truth.
- **`apps pull` user-set secret env count no longer double-counts platform-injected keys**: `envVarCount` in the pull JSON summary used to sum plain + secret without filtering. Now `envVarCount` matches `apps_env-vars` keys exactly, and `secretEnvVarCount` reflects user-set secret keys only.
- **`apps diff` `--redact-secrets` covers the secret env section**: values in the `SecretEnv` `SectionDiff` are redacted in the unified-diff output by the same flag that redacts `.env` content. The MCP `apps_diff` tool keeps passing `--redact-secrets`.
- **Windows build**: `syscall.O_NOFOLLOW` (used in `apps_pull`'s symlink-refusal write path) doesn't exist on Windows. Replaced with a build-tagged constant: protection is preserved on Unix; the flag is a no-op on Windows where the equivalent OS-level guarantee isn't available.
- **`runos deploy` auto-detects pulled yaml in cwd when no `-c` and no `runos.yaml`** (I15-A): pre-fix, deploy emitted a cryptic "runos.yaml not found at <path>" while `apps diff` already had a candidate-enumeration scanner. Deploy now scans the cwd for `runos.<cid>.<id>.yaml` (both `apps.YAMLScan.Valid` and `.Partial`), auto-picks a unique hit, and surfaces a candidate list mirroring `apps diff`'s message on multi-hit. Zero-hit falls through to the original LoadConfig error.
- **Manifest-driven commands accept yaml body via stdin (`-f -`)** (I15-B): pre-fix, the pre-network required-field gate and the RunE body collector each opened stdin independently, so the first reader drained the pipe and downstream gates fired "missing required field". `loadYAMLFile` now sync.Once-caches a 10MiB-capped `io.LimitReader(os.Stdin, ...)` read so all three call sites see the same parsed body.
- **`--json` error envelopes forward unknown top-level fields verbatim** (I15-C): pre-fix, `BuildAPIErrorEnvelope` extracted only `error` from the inner body and synthesised `statusCode`, dropping any structured detail the server emitted (e.g. an `upstream: {provider, status, body}` block from a future cloudflare-style error shape). Forward-compat pass-through copies every top-level inner-body key into the envelope, with `error` (overridden from the inner string) and `statusCode` (authoritative from the typed `*APIError`) protected. Future conductor-side error-detail keys surface to MCP/JSON consumers without a CLI release.
- **MCP executor strips `"<cid> (Cluster Name)"` display label off body-field cids** (I16-C): pre-fix, `extractCID` ran for URL-path substitution but the un-stripped value remained in `args["cid"]`, so account-scoped POSTs that carry `cid` as a body field (canonical case: `cluster-domains/add` against `/:aid/cluster-domains`) sent the literal display label to the conductor. Conductor accepted it unchecked and the downstream CP lookup against the bogus id surfaced as a misleading "no control-plane nodes are registered" error. `Execute` now mirrors the stripped value back into `args["cid"]` before `buildBody` runs. Conductor 12.4.0's server-side `clusterRepository.findById` validation + 12.5.0's `normalizeCid` accept-both-forms helper stand as defense in depth.
- **MCP cid descriptions no longer prompt models toward the parenthesised display form**: with conductor 12.5.0 normalising both `"mycluster2"` and `"mycluster2 (Cluster-2 mycluster2)"` and CLI R8 stripping the suffix client-side, the verbose `"format 'xyz (Cluster Name)'"` guidance in MCP tool descriptions was actively pushing models into a form with no remaining functional purpose. Descriptions across `apps_pull` / `apps_diff` / `apps_sync` / `apps_list_previous_uploads` / `services_pull` / `services_diff` / `services_sync` / `deploy` / the generic `:cid`-endpoint wrapper now read simply `"Cluster ID. Defaults to the configured default cluster."` (or `"Cluster ID. Cross-checked against the yaml's own cid field."`).
- **MCP `cluster-domains_show id=runos` returns the actionable synthetic-runos redirect** (I17-A): pre-fix, the cobra/dynacmd path intercepted `cluster-domains show --id runos` with `"\`runos\` is a synthetic per-cluster cluster-domain; use \`runos cluster-domains list-by-cluster --cid <cid>\`..."` (I11-W) but the MCP executor ran past that gate, so MCP/LLM consumers got a bare conductor 404. The MCP executor now mirrors the gate with an MCP-tool-name-appropriate phrasing (`cluster-domains_list-by-cluster` tool instead of the cobra invocation).
- **Service-type logs gain `--follow` and duration `--since` parity with `apps logs`** (I17-F): pre-fix, the iter-14 I14-E (`apps logs --follow` poll-2s stream) and I14-F (`apps logs --since 5m` duration-string parsing) fixes landed on `apps/logs` only. Every `services/<type>/{id}/logs` reader (postgresql, valkey, mysql, harbor, prometheus, litellm, clickhouse, vector, cert-manager, traefik, rabbitmq, kafka, grafana, ollama, vllm, netbird-server, netbird-client, minio) had the same wire shape but missed the `--follow` flag, the `--since 5m` duration form, and the `--tail` k8s-floor description suffix. Introduced `isPodLogsCommand(cmdPath)` predicate (matches `apps/logs` + every `services/<type>/{id}/logs`) and replaced the four hard-coded `cmdDef.Command == "apps/logs"` match sites in the builder + executor. `followAppsLogs` renamed `followPodLogs` since the poll loop is endpoint-agnostic. `builds/logs` deliberately excluded (different shape: limit-bounded, object envelope).
- **`runos deploy` cross-checks yaml's `cid:` against `--cid`/config cid** (I18-B): pre-fix, `apps diff` already refused on mismatch (`bindToYAML` in cmd/apps.go) but `runos deploy` silently let the flag/config cid win and ignored the yaml. A stale `--cid` against a directory-per-app yaml could push to the wrong cluster. New `deploy.ReconcileCID(callerCID, yamlCID)` helper (sibling to `ValidateAID`) returns the resolved cid or refuses with the verbatim apps_diff error message; wired into both `runDeploy` (replaces the fill-only block right after `LoadConfig`, gating both CLI- and VCS-deploy dispatch) and `runDeploySync` (inserted before the previously-blind `deployConfig.CID = cid` overwrite).
- **`runos config set` validates per-key constraints** (I18-J): pre-fix, the setter accepted any string for any key, including `runos config set cid ""` which silently broke every downstream command that fell back to the default cluster. New `validateConfigSet(key, value)` enforces cid against conductor's `normalizeCid` regex (`[a-z0-9]{3,16}`) with an actionable hint pointing at `runos clusters list`, and console-url/api-url require non-empty + http/https scheme + host via `net/url.Parse`.
- **Pod-logs `--since` capped at 90 days client-side** (I19-G): pre-fix, `apps_logs --since 9999999999` (absurdly large seconds value) returned a bare 500 from the conductor's int handling. New rule in `validateInputValues` rejects pre-network when `since > podLogsSinceCapSeconds` (7,776,000 s = 90 days), with an actionable refusal naming the cap + day equivalent. Predicate-driven via `isPodLogsCommand` so apps/logs + every services/<type>/{id}/logs inherits the gate without per-type code.
- **`runos deploy --json` returns a parseable JSON envelope on success path for VCS deploys** (I24b-A): pre-fix, the VCS branch (`runDeployVCS`) printed text on stdout (`"Deploying app X @ Y..."` / `"Deployment initiated:"`) while only the error path emitted JSON, so `jq .jobId` got a parse error on every successful VCS deploy. The CLI-deploy branch (`runDeploy`) was already correct. `runDeployVCS` now threads `jsonOutput` through; emits `vcsDeployJSONResponse{jobId, appId, sha, configPath, publicUrl?}` to stdout under `--json` while preamble, configPath note, and follow progress route to stderr via new `jobs.FollowJobToWriter` / `FollowJobWithServiceToWriter`. Pairs with the I24-I banner-after-API-2xx fix so a submit-time API rejection (malformed sha, missing app, etc.) doesn't print a misleading "Deploying..." preamble.
- **Deploy banner prints after the API 2xx instead of before** (I24-I): pre-fix VCS deploy emitted the `Deploying app X @ Y...\n  configPath: ...\n` preamble immediately, before the submit-time API validation. Users saw "Deploying..." on a `--sha xyz` (malformed) call that the API then rejected with a 400 — confusing because the operation never actually got past the API. Banner now prints after `svc.DeployVCS(...)` returns 2xx.
- **`-f` / `--file` is uniformly accepted on every dynacmd command** (I24-G): pre-fix the flag was gated on `hasNonPositionalInput` so body-less commands (e.g. `apps replace-manifest`, which takes only the positional app id) rejected `-f /tmp/x.json` with cobra's misleading `unknown flag --file`. The I11-U doctrine ("don't silently ignore user file content") is preserved via a new executor-side gate in `collectInput`: when `-f` is passed AND the command has no non-positional inputs, refuses with `command "X" has no body input; the -f / --file flag is inert here. ...` naming the command and pointing at `--help`. Net effect: every body-less command surfaces the same clear message instead of "unknown flag".
- **`apps pull` no longer writes `sourceDir: ..` on VCS apps** (I24-H): pre-fix, id-subdir pull mode (the default for `--id`) stamped `sourceDir: ..` regardless of deployType, intended for the multi-cluster directory-per-app shape on CLI-deploy projects. For VCS apps, sourceDir is a CLI-deploy concept (build-context root for tarball uploads / `apps_pull --code`) — has no meaning at deploy time because conductor pulls source from the linked git integration at <sha>. The ".." default leaked into VCS yamls and pointed the field at the pull's parent directory (canonical case: `/tmp/x` containing arbitrary tmp content). `pickSourceDir` now takes deployType; for VCS apps the caller default is overridden to "" so the field stays omitempty unless user or server has an explicit value. CLI-deploy apps keep the historical ".." default.
- **`apps sync --json` is accepted in apply mode, not just `--dry-run`** (I24-M): pre-fix, `runos apps sync --json` (apply) refused up-front with `"--json is only valid with --dry-run"`, breaking CI gates that wanted machine-readable confirmation of the applied sync. Apply mode now emits a JSON envelope to stdout at the end of the apply (`{appId, appName, cid, jobIds[], followed, completed}`); progress lines route to stderr so stdout stays pure JSON. Under `--follow + --json`, per-step job progress also routes to stderr via the new `FollowJobWithServiceToWriter`.
- **CLI text output surfaces unknown top-level response fields** (I24-D): pre-fix, the output formatter's manifest-driven path used the declared `output.fields` list as a strict filter, so any field the conductor added to a response after the CLI's manifest snapshot was silently dropped from text mode. Canonical case: conductor 14.2.0 added a `gitlabRunner: {...}` block to `integrations/gitlab-runner/status` (with `contactedAt`, `tagList`, `online`, `paused`, `runUntagged` from the live GitLab API), but `runos integrations gitlab-runner status <id>` continued to show only the historical fields. The formatter now appends unknown top-level keys at the end alphabetically (same I15-C doctrine as error envelopes), and renders nested objects as indented blocks instead of single-line `map[...]` mash. JSON mode already worked.
- **`apps pull` surfaces the conductor's per-app `sourceHint` for VCS apps** (I24b-C): conductor 14.2.0 added a `sourceHint` field to the apps_show response (canonical content for VCS apps: `"VCS app — code lives at <repoUrl>@<branch>. Run 'git clone <repoUrl>' to edit locally; the cloned tree is the build source."`). `pulledAppEntry` gains a `SourceHint` JSON field; `pullOne` extracts it from the apps_show raw response; `printUpdatedApp` prints it as the trailing per-app line. JSON consumers see it automatically.
- **`runos --version` / `-v` / `runos version` all emit the bare version string** (I24-Q): pre-fix cobra's auto-generated `--version`/`-v` rendered `"runos version <X>"` while the explicit `runos version` subcommand printed just `"<X>"`. CI gates piping through one shape stripped a prefix that the other didn't have. `rootCmd.SetVersionTemplate("{{.Version}}\n")` aligns all three forms on the bare format. Picked bare over prefixed because the subcommand's bare contract is the side scripts already depend on.
- **`apps sync --json` apply mode emits the post-apply envelope** (I24-M follow-up): pre-fix the dry-run-only JSON emit branch fired unconditionally, so `apps sync --json` (without `--dry-run`) emitted the plan struct and returned before applying. Now gated on `jsonOut && dryRun`; apply mode falls through to `applySyncPlan` which emits `{appId, appName, cid, jobIds[], followed, completed}` on stdout while the rollout footer + per-step progress route to stderr. Plan preview is suppressed under apply+jsonOut (the per-step "ok (job X)" lines on stderr already cover visibility).
- **`runos deploy --json` (VCS) banner routing pinned with regression tests** (I24b-A follow-up): the preface banner + configPath line under `--json` route via the `human` writer (os.Stderr); extracted `vcsDeployStreams(jsonOutput) (stdout, human)` and `printVCSDeployBanner(human, ...)` so the routing contract has unit-test coverage (the runDeployVCS integration path requires a live deploy.Service and can't be exercised without a real cluster). Future refactors that let the banner leak to stdout under `--json` fail `TestVCSDeployStreams` / `TestPrintVCSDeployBanner`.
- **`runos apps add --provision-ci-variables` flag removed** (iter-25-R2): conductor 15.0.0 rolled back the auto-provision CI flow as a design decision (per-app PAT lifecycle + monorepo blast radius weren't worth the surface area; industry standard is manual PAT setup via `runos account api-keys add`). The I24-U flag was a no-op against any conductor running 15.0.0+ and would have become a 400 in a later cut. The CLI-side `extraFieldsFor("apps/add")` entry is gone; the hook remains in place for the next field that arrives ahead of its manifest entry. Migration: drop `--provision-ci-variables` from any scripts; mint a PAT via `runos account api-keys add` and set RUNOS_API_KEY / RUNOS_ACCOUNT_ID / RUNOS_API_URL on the linked CI project once per account (the same PAT drives every app the account owns).
- **`runos follow <jobId>` pre-validates UUID shape** (I25-V): pre-fix, a typo like `runos follow not-a-uuid` fell through to a network call and surfaced as `Error: job not found: not-a-uuid` (HTTP 404 from conductor). New pure helper `jobs.ValidateJobID(id)` enforces the canonical 8-4-4-4-12 hex pattern client-side; the cobra layer refuses pre-network with `invalid job id "X": expected UUID shape (8-4-4-4-12 hex). Use 'runos jobs list' to find the id you want`. Regression test `TestValidateJobID` covers 9 cases (lowercase, uppercase, empty, missing dashes, off-by-one length, non-hex, slug-shape, sentinel).
- **`runos follow` no longer dumps cobra Usage on terminal job failure** (I25-AB): pre-fix, `runos follow <jobId>` on a job that hits terminal `failed` printed `Error: job failed: ...` then cobra's full `Usage:` block (15+ lines of flag descriptions), drowning the actual error. Job failure isn't an args/flags problem; `SilenceUsage: true` is set on the follow command so the user sees the error and only the error. Exit code stays non-zero, propagated by cobra.
- **`runos apps sync` non-tty confirm: skip the prompt instead of EOF'ing** (I25-AE): pre-fix, `runos apps sync runos.yaml` without `--yes` on a non-tty produced `Apply changes to X? [y/N] Error: read confirmation: EOF` followed by 16 lines of cobra Usage. Now mirrors `runos deploy` and `runos services sync`: when stdin is not a terminal (CI / piped input), the prompt auto-skips since no human can see it. `--yes` remains the explicit opt-out for TTY users. The empty-secret-env wipe gate and other safety refusals are unaffected. `SilenceUsage: true` also set so the Usage block can't reappear via any other confirmation-EOF path.
- **`runos apps sync --json` help text matches post-I24-M reality** (I25-AF): pre-fix the flag description still claimed "only valid with --dry-run" even though I24-M had extended `--json` to apply mode (emitting the post-apply jobId envelope). Help text now describes both behaviours: dry-run emits the structured plan, apply emits the per-step jobId envelope; progress lines route to stderr.
- **VCS deploy dirty-tree gate skipped when `--sha` is explicit** (I25-Y): pre-fix, `runos deploy --app X --sha Y --cid Z` from inside a dirty git checkout refused with `working tree is dirty; commit changes or pass --allow-dirty` even though the yaml wasn't consulted and the build runs against the committed source at `<sha>` on the git host. Local working tree state is by definition orthogonal in that mode. New `shaProvided` capture: the gate only fires when sha was auto-derived from HEAD. When the caller supplies `--sha` explicitly the gate skips entirely, eliminating the spurious refusal on CI-shaped invocations from laptop checkouts.
- **Empty PAT-shape env vars refused early** (I25-G / I25-J): pre-fix, `RUNOS_API_KEY=''` or `RUNOS_ACCOUNT_ID=''` (set-but-empty, the canonical case being a CI secret-store expansion typo like `$VAR_THAT_DOES_NOT_EXIST` resolving to empty) fell through to the cached Firebase token / config.json AccountID silently — runners that intended a PAT flow got unexpected success using the developer's stored credentials. New `auth.ValidateAuthEnvVars(lookup)` distinguishes unset (fine; falls back to interactive auth) from explicitly empty (refuse with `RUNOS_API_KEY is set but empty; either unset it to fall back to interactive auth, or set it to a real value`). Wired into `PersistentPreRunE` so every command sees the gate before any auth path resolves. Regression test `TestValidateAuthEnvVars` covers 6 combinations.
- **401 auth errors carry actionable recovery hints + revoked distinction** (I25-H / I25-I): pre-fix, every 401 surfaced as a bare `Error: API error (401): {"error":"Invalid token"}` regardless of whether the PAT was malformed, revoked, or pointed at the wrong environment. New `formatAuthError` hook in the dynacmd error path renders `authentication refused (401): <msg>` followed by a 3-line checklist (verify `RUNOS_API_KEY`, verify `RUNOS_API_URL` matches the PAT's environment, mint a fresh PAT). When conductor supplies a `code` field (e.g. `REVOKED`) or `revokedAt` timestamp, both surface distinctly so CI logs spell out "revoked at <ts>" instead of conflating revoked with malformed. Server-side extension point: any future structured signal in the 401 body is picked up automatically. Regression test `TestFormatAuthError` covers 4 cases.
- **Hand-authored yaml routing to existing VCS app surfaces the VCS shape** (I25-M): pre-fix, a `runos.yaml` with only `app: <name>` (no `id`, no `deployType: vcs`) sent to `runos deploy` routed to the CLI-deploy "create new app" code path and produced `An app named 'X' already exists (ID: <appid>). Run 'runos deploy sync' to link to existing app, or rename the app in runos.yaml.` — but `deploy sync` is a CLI-deploy verb that doesn't make the VCS shape obvious. `AppInfo` now carries `DeployType`, and `cmd/deploy.go` branches on it: VCS-shaped existing app surfaces a 3-option remediation hint (`runos apps pull --id <id>` to fetch the canonical yaml, or hand-add `id: <id>` + `deployType: vcs`, or deploy directly via `runos deploy --app <id> --sha <sha>`). CLI-shaped path keeps the legacy hint.
- **Manifest fetch survives version-endpoint failures on fresh installs** (I25-D): pre-fix, on a fresh `~/.runos/` the manifest loader's first call hit `/cli/manifest-version` to decide whether to refresh the local cache; when that endpoint failed (older conductor without the version route, transient gateway error, wrong `RUNOS_API_URL`), the loader gave up with `no manifest available: failed to read manifest: open ~/.runos/manifest.json: no such file or directory`, even though `/cli/manifest` itself would have worked. Now falls through to `fetchManifestRaw` when version-check fails AND there's no local cache; the manifest is saved to disk so subsequent runs short-circuit normally. The combined error (when both fall throughs fail) names both root causes so the user isn't chasing a phantom "manifest missing" when the underlying issue is reachability.
- **Recovery hint for missing config covers PAT path** (I25-F): pre-fix, `ErrConfigNotFound` named only the interactive recovery path (`runos config env <environment>`) — but PAT-driven CI runners don't run that flow, they set `RUNOS_API_URL` + `RUNOS_API_KEY` + `RUNOS_ACCOUNT_ID`. The error now lists both paths explicitly so CI users aren't pushed toward an interactive Firebase login they don't need.
- **Manifest-load failure at init time names the disabled surface** (I25-E): pre-fix, when `registerDynamicCommands` failed (canonical case: fresh install where the version-endpoint hiccup landed before I25-D's fallback), the user-facing failure for a subsequent `runos apps show --cid X --id Y` was a misleading `unknown flag: --cid` because the dynacmd commands had silently dropped out of cobra's tree. The init-time stderr now spells out that dynamic commands (apps show / services list / integrations * / account * ...) are unavailable for this invocation AND names the two recovery paths (`runos manifest update` or verify `RUNOS_API_URL` / `RUNOS_API_KEY`). The downstream "unknown flag" error remains structurally unavoidable without restructuring cobra init ordering; this fix at least makes the root cause visible above the misleading symptom.
- **`deploy.AppInfo` carries `deployType`**: prior shape decoded only id/name/port from `GET /apps`, so callers that needed to branch on deployType had to make a follow-up `GET /apps/:id`. The new field is populated by conductor's apps-list response and consumed by I25-M's hand-authored-yaml branching.
- **401 renderer reads conductor 14.9.0's `reason` field** (I25-H/I follow-up): the initial I25-H/I fix anticipated server-side `code` + `revokedAt` extension fields, but conductor 14.9.0 landed `reason: 'revoked' | 'expired'` plus the matching `revokedAt` / `expiredAt` timestamp instead. `formatAuthError` now matches the actual wire shape: a `reason=revoked` body renders `authentication refused (401): Invalid token [revoked at <ts>]`; `reason=expired` renders `[expired at <ts>]`. The `error: "Invalid token"` string is preserved for backwards compatibility. Unknown reasons surface bracketed as-is so any future enum value (e.g. `reason: "scope-denied"`) is visible without a CLI release. Regression test `TestFormatAuthError` extended with the new wire shape.
- **`apps sync` surfaces conductor 14.9.0's apps-update no-op** (I25-AD): pre-fix, the apps_sync apply path always reported `(job <id>)` from the yaml-PATCH step even when conductor short-circuited the redeploy because the body equalled the stored AppDocument. Hot loops produced phantom job IDs whose follow-status was always "completed instantly" — confusing because no actual rollout ran. Conductor 14.9.0 now returns `{ osid, noOp: true, message }` HTTP 200 for the no-change case; CLI surfaces it as `yaml: PATCH no-op (<message>)` and skips both the job-tracking and the follow-wait. New `Service.UpdateAppFull` returns the structured ack alongside the legacy jobID; `Service.UpdateApp` stays a thin wrapper for callers that only need the id. Regression tests `TestService_UpdateAppFull_NoOpResponse` + `TestService_UpdateAppFull_QueuedJob` pin both response shapes.
- **Array-typed manifest flags accept JSON values verbatim** (I25-B): pre-fix, `runos apps service-port-mappings set --service-port-mappings '[{"port":3000,"standardHttps":true}]'` was rejected with `parse error on line 1, column 2: bare " in non-quoted-field`. pflag's `StringSlice` split the value on the `,` inside the JSON, then choked on the `"` it found in what it thought was a CSV field. The dynacmd builder now binds `array` fields via pflag's `StringArray` (each `--flag <value>` invocation taken verbatim, no CSV splitting), and the executor passes the collected slice through a new `coerceArrayFlagValue` helper that JSON-decodes the elements when possible: single-invocation JSON arrays unwrap to a real `[]any` on the wire; repeated `--flag '{...}' --flag '{...}'` becomes a `[]any` of objects; bare-string lists (`--tags a --tags b`) pass through as `[]string`. Mirrors the MCP executor's existing `coerceJSONString` so CLI and MCP behave identically on object/array inputs. Regression test `TestCoerceArrayFlagValue` covers 5 cases (single JSON array, repeated JSON, bare strings, empty, mixed-fallback).
- **Cobra Usage block silenced when manifest pre-flight fails** (I25-E): pre-fix, when `registerDynamicCommands` failed at init time, the user-facing failure for a subsequent `runos apps show --cid X --id Y` was `Error: unknown flag: --cid` followed by cobra's 16-line `Usage:` block (showing only the four static `apps` subcommands `diff list-previous-uploads pull sync` because every dynacmd-registered leaf had silently vanished). The manifest-fail diagnostic sat three screens back, drowned by the noise. Setting `rootCmd.SilenceUsage = true` + `rootCmd.SilenceErrors = true` when the init-time manifest load fails strips both the misleading flag-parse error and the truncated Usage block from cobra's fallback path; the actual root-cause diagnostic (already emitted to stderr, naming the disabled surface and the recovery commands) is what the user reads. Non-zero exit code preserved for CI gates.
- **`runos account api-keys add` defaults `--expires-at` to 90 days** (I25-AP): pre-fix, conductor's `apps_add` setupInstructions response surfaced a `createPatCommand` like `runos account api-keys add --name <X>` for users to copy-paste — but the CLI marks `expiresAt` as required (and conductor's `createPatCommand` deliberately omits expiry because rotation cadence is caller-policy). Verbatim copy-paste failed with `missing required argument: expiresAt (pass --expires-at, or include expiresAt: in -f file)`. The CLI now injects a 90-day RFC3339 default at the top of the dynacmd RunE, before the missing-required gate fires, when `--expires-at` is omitted AND no `-f` body file supplies `expiresAt:`. Explicit user input (flag or `-f`) wins. 90d matches the api-keys topic's documented rotation cadence and conductor's documented `limited`-role default. New `applyCLIDefaults` helper hooks the carve-out by command path so future "createPatCommand"-style usability gaps land in the same place. Regression test `TestApplyCLIDefaults_APIKeysAddExpiresAt` covers default-injection, explicit-wins, and other-command-untouched.
- **Env-vars endpoints accept the conductor 16.0.0 `{envVars: {...}}` envelope** (I26-O, regression): pre-fix, conductor flipped `GET /apps/:id/secret-env-vars` (and the plain `/env-vars` sibling) to return an envelope object as part of the 16.0.0 response-shape direction. The CLI's struct still expected the legacy bare `{KEY: value, ...}` shape and rejected every response with `failed to parse response: json: cannot unmarshal object into Go value of type string`. This took the entire IaC reconciliation surface (`apps pull` / `apps diff` / `apps sync` / `runos deploy`'s pre-sync env merge) offline until the CLI was bumped. New shared parser `parseEnvVarsResponse` in `internal/apps/service.go` (mirrored in `internal/deploy/service.go` to preserve the package-dependency boundary) accepts both shapes: a single-key `{envVars: ...}` object unwraps to its inner map; everything else parses as the bare legacy map. Forward-compat with the older bare-map endpoints so the CLI rounds-trips both during the transition window. Regression tests `TestParseEnvVarsResponse` + `TestService_GetAppSecretEnvVars_AcceptsEnvelope` + `TestService_GetAppSecretEnvVars_AcceptsBareMap` + `TestParseEnvVarsResponse_DeployCopy` pin both shapes against fake conductor handlers.
- **Missing-required hint omits unregistered flag form** (I26-S): pre-fix, the dynacmd error for a missing required field always read `<field> (pass --<flag>, or include <field>: in -f file)` regardless of whether the flag actually existed. Object-typed fields like `envVars` on `apps/env-vars/set` have no good flag form (the builder type switch skips object), so the suggestion pointed at a `--env-vars` flag that cobra then rejected as unknown. The user landed in a loop: required-arg error → suggested flag → unknown-flag error → look at `--help` → neither the field nor its flag are shown. The new `formatMissingFieldHint` helper probes cobra's registered flags before naming the flag form; when no flag exists, the message recommends only the `-f body.yaml` path with the canonical field name. Regression test `TestFormatMissingFieldHint` covers both branches.
- **`--help` lists object-typed body fields with a `-f` discoverability note** (I26-P): pre-fix, object-typed body fields with no flag form (`requires` on `apps/update`, `envVars` on `apps/env-vars/set`, etc.) were effectively undiscoverable on PATCH-shape endpoints where the field is optional and the missing-required gate never fires. Users reaching for `runos apps update --requires <...>` got "unknown flag" with no breadcrumb to the actual surface. The new `buildLongDescription` helper extends cobra's Long help text on every dynacmd leaf: when the manifest declares any object-typed body field, the Long block lists each one ("`requires` (required) — Service dependencies (alias → {id, type, config, env})") under a "Body fields without a flag form (pass via -f body.yaml):" header so `runos <verb> --help` carries the full input surface. Empty when no object fields are declared (no change for the common case). Regression test `TestBuildLongDescription` covers presence/absence, required marker, and positional-object skip.
- **List-style endpoints accept envelope responses on every consumer** (I26-O / I26-U follow-up): the initial I26-O fix covered `apps_secret-env-vars` + `apps_env-vars`; iter-26-R1 surfaced that conductor's envelope-everywhere direction had also wrapped `apps_list` (`{apps: [...]}`), `jobs_list` (`{jobs: [...]}`), `services_postgresql_users` (`{users: [...]}`), `apps_overrides_list` (`{overrides: [...]}`), `apps_logs` (`{entries: [...]}`), and `apps_network-access` (`{endpoints: [...]}`). The CLI's bare-array consumers broke in turn — `apps_pull` failed with `cannot unmarshal object into Go value of type []apps.AppSummary` because `Service.ListApps` decoded directly into `[]AppSummary`. New shared `unmarshalListResponse(data, key, out)` helper in `internal/apps/service.go` (mirrored as `unwrapArrayEnvelopeDeploy` in `internal/deploy/service.go` and `unwrapArrayEnvelope` in `internal/output/formatter.go` + `internal/dynacmd/executor.go`, to keep package-dependency boundaries one-way) accepts both shapes — single-key object whose value is an array unwraps to the inner array; bare top-level arrays pass through unchanged. Routes: `apps.Service.ListApps`, `apps.Service.ListOverrides`, `deploy.Service.FindAppByName`, `output.Formatter.formatArray` (text-mode rendering of every dynacmd list endpoint), and the dynacmd executor's pod-logs follow loop (`seedSeenSet` / `filterUnseenLogEntries`). Regression tests `TestUnmarshalListResponse` + `TestUnwrapArrayEnvelope` + `TestService_ListApps_AcceptsEnvelope` pin both shapes against fake conductor handlers. IaC reconciliation surface (pull / diff / sync) and CI consumers of every list endpoint round-trip the rollout window cleanly.
- **Ambiguous `key=value` form refused for object-array fields with a pointer to JSON** (I26-E CLI side): pre-fix, `runos apps service-port-mappings set --service-port-mappings 'port=3000'` (the obsolete I25-B workaround, since the I25-B fix landed StringArray + JSON coercion natively) passed the literal string through to the wire as `["port=3000"]` and conductor refused with `servicePortMappings[0] must be an object (got string)`. The conductor message is correct but doesn't tell the CLI user what to type. New pre-network gate `refuseAmbiguousKeyValueArray(flagName, raw)` runs inside `collectInput`: any element containing `=` that doesn't parse as JSON surfaces `--<flag>: element "<elem>" looks like \`key=value\` shape, but this field expects structured objects. Pass JSON instead, e.g. --<flag> '{"port":3000,"standardHttps":true}' (single object) or --<flag> '[{...},{...}]' (array). Repeat the flag to add multiple elements.` Skipped on `format: "key_value"` fields (tag-style endpoints use colon-separated `key:value` semantics, handled by `parseKeyValueTags`). Legitimate bare-string lists (`--tags one --tags two`) and JSON elements both pass. Regression test `TestRefuseAmbiguousKeyValueArray` covers 7 cases including mixed JSON + k=v fallthrough.
- **MCP tool schemas declare typed `additionalProperties` for map-of-object fields** (I26-N): pre-fix, every `object`-typed manifest field projected to MCP as `additionalProperties: {type: "string"}`, with a single carve-out for `providerOptions`. `requires` (apps/add / apps/update / deploy) is the canonical map-of-struct field — alias → `{id, type, config, env}` — and LLM clients following the schema strictly stringified the payload, then conductor rejected with `requires.X must be an object`. Manifest.Field gains `ValueType` + `ValueFields` (parallel to the existing `ItemType` + `ItemFields` for arrays); the MCP server's new `projectObjectValue` helper emits the rich schema when the manifest declares them. As a transition-window bridge until conductor's manifest ships ValueType/ValueFields for `requires`, the same helper carries a hardcoded fallback for that field name only (id+type required, config+env optional, both `map[string]string`). The fallback drops out automatically the moment the manifest declares its own ValueType. The legacy `{type: "string"}` default still applies to every other object field, preserving back-compat for the existing map[string]string fields whose manifest entries pre-date ValueType. Regression test `TestProjectObjectValue` covers all four branches (providerOptions drop, manifest-driven, requires hardcode, default fallback) plus the manifest-wins-over-hardcode contract.
- **`runos <verb> --help` self-consistent on object-only body fields** (I26-P, closed via I26-S resolution pattern): `runos apps update --help` now lists `requires` under the Long block's "Body fields without a flag form (pass via -f body.yaml):" header (shipped earlier as `buildLongDescription`), and conductor 17.6.1's manifest description appends an in-line "This field has no `--requires` flag; pass via `-f body.yaml`" recovery hint. Both signals visible in a single `--help` output. No flag form is exposed (decision: keeping the surface small; `-f body.yaml` is the documented path for object-shaped body fields). The same pattern closed I26-S in the previous round.
- **VCS deploy `configPath` fallback line uses calm phrasing** (I27-H): pre-fix, every successful VCS deploy emitted `configPath: <not sent> — using whatever the AppDocument has stored` on the post-2xx banner. The phrasing read like an error/warning ("not sent" + "whatever") for a completely normal code path (CI deploys with `--app` and no yaml on disk delegate configPath resolution to the AppDocument by design). The line is now `configPath: (using AppDocument default)`, a calm declarative status with no alarmist hedge. Regression test `TestPrintVCSDeployBanner` updated to assert both branches: non-empty configPath prints the explicit path with no fallback hint, empty configPath prints the calm fallback with no `<not sent>` substring.
- **MCP `services_pull` maps `service_id` schema field to CLI `--id` flag** (I27-L): pre-fix, calling `mcp__runos__services_pull cid=mycluster2 service_id=mysvc type=postgresql out=<dir>` surfaced `services_pull failed: Error: unknown flag: --service-id`. The MCP server's `buildServicesPullArgs` translated the schema's `service_id` field to a `--service-id` CLI argument, but `runos services pull` registers only `--id` (no `--service-` prefix). Disambiguation in the schema (so the LLM doesn't confuse it with `cid` / `aid`) shouldn't leak into the wire shape. The argv translator now emits `--id <sid>` regardless of the schema field name. Regression test `TestBuildServicesPullArgs_ServiceIDMapsToIDFlag` asserts the argv never contains `--service-id` and `TestBuildServicesPullArgs_PassesForceAndYAMLPositional` pins the `--force` + `-- <yaml>` tail.
- **`runos deploy --json` VCS-conflict path is pure JSON** (I27-P): pre-fix, when `runos deploy --json` hit the "yaml is missing the VCS shape" remediation path, the 4 lines of plain-text guidance (`An app named 'X' already exists on this cluster...`, `Hand-authored yaml is missing the VCS shape. Either:`, plus the 3 numbered fix options) wrote to stdout before the deferred `emitJSONError` envelope, so `jq .` parsing on the consumer side hit `parse error: Invalid numeric literal at line 1, column 3`. The guidance now routes through the existing `progress()` writer (stderr under `--json`, stdout otherwise), keeping stdout pure JSON for the JSON envelope while preserving the human-readable guidance on stderr for TTY callers. The legacy CLI-deploy "Run 'runos deploy sync' to link" hint gets the same routing.
- **`unwrapArrayEnvelope` covers conductor 17.7.0's list-endpoint migration** (I27-T verification): conductor 17.7.0 wrapped the last five bare-array list endpoints (`domains`, `cluster-domains`, `integrations`, `nodes`, `services_<type>_list`) in single-key envelopes (`{domains:}`, `{clusterDomains:}`, `{integrations:}`, `{nodes:}`, `{services:}`) to complete the iter-26 envelope-everywhere migration. The CLI's `internal/output/formatter.go:unwrapArrayEnvelope` helper has been shape-keyed (any single-key object whose value is an array) rather than key-keyed since I26-O, so the text-mode formatter already renders the new shapes without any code change. The `--json` consumer surface continues to pass the response through verbatim (so callers see `{domains: [...]}` not the bare array; this matches the iter-27-R0 expectation already validated for `apps list` / `jobs list` / `apps overrides list`). New sub-test `iter-27 envelope keys all unwrap` enumerates all five new keys against the helper to self-document the closure and catch any future regression that special-cases envelope key names.
- **Topic docs**: `gitignored/mcp-topics/applications.md` gains two notes inline with the existing "sourceDir and dockerfile" section: (I27-M / I27-N) pulled yaml is the `apps_update` body shape, not a faithful copy of the committed yaml — `configPath`, `sourceDir`, and `dockerfile` are deliberately omitted because they live on the AppDocument and committed yaml respectively, not on the drift-tracked surface; editing them in the pulled file silently no-ops on `apps_sync` and `apps_diff` won't surface drift. (I27-G) a new "Static-site Dockerfile templates (non-root constraint)" subsection documents that `nginx:alpine` won't start as a non-root user on a RunOS cluster (binds port 80) and recommends `nginxinc/nginx-unprivileged:1.27-alpine` as the drop-in for static sites, ports already aligned to 8080.
- **VCS deploy 4xx returns typed `*APIError` so `--json` flattens cleanly** (I27-Z): pre-fix, `runos deploy --app X --sha <bad-sha> --json` produced a doubly-nested envelope where the outer `error` key was a string concatenating `"failed to trigger VCS deploy: API error (400): "` plus the inner conductor body as escaped JSON text (`"{\"error\":\"Commit ... not found\"}"`). CI consumers piping into `jq .error` got the top-level message but lost the structured inner fields and had to peel two layers of JSON. `internal/deploy/service.go:DeployVCS` now returns `&APIError{StatusCode, Body}` on `resp.StatusCode >= 400` (matching the existing `FindAppByName` / `GetApp` shape in the same file), so `cmd/apps_pull.go:emitJSONError`'s existing `errors.As(err, &deployErr)` fallback unmarshals the inner body and emits the canonical flat envelope `{"error": "<conductor msg>", "statusCode": 400}` instead. Single source of truth for the error shape across CLI and VCS deploy branches. Regression test `TestDeployVCS_ReturnsTypedAPIErrorOn4xx` pins the typed-error contract against a fake conductor 400 response.
- **`runos apps logs --tail 0` refused client-side on pod-logs verbs** (I27-AB): pre-fix, `--tail 0` was accepted by the integer-validator's non-negative gate and forwarded to the API, which forwards it to kubelet, which interprets `--tail=0` as "use default" rather than "return zero entries" — so the caller asking for zero log lines got exactly one back. The new branch in `validateInputValues` refuses `--tail 0` on `isPodLogsCommand` verbs with `--tail 0 is ambiguous (kubelet treats it as 'use default', returning ~1 entry). Pass --tail >= 1 to limit the slice, or omit --tail for the default`. Non-logs integer fields are untouched (e.g. `replicas: 0` for scale-to-zero remains valid). Regression test `zero tail refused on pod-logs (I27-AB)` flipped the prior `zero tail OK` expectation; the negative-int regression case stays in place.
- **Boolean flag space-form error suggests equals-form** (I27-AC): pre-fix, `runos apps overrides update --id ultbd --enabled false` surfaced as `ambiguous id: positional "false" and --id="ultbd" disagree; pass only one` because cobra parses `--enabled` as a no-value boolean (true by presence) and the next token (`false`) lands in the positional slot, colliding with `--id`. The error pointed at `--id`, leaving the user no breadcrumb to the actual cause (space-form on a boolean flag). The error now appends `If you meant a boolean flag, use --<flag>=false (equals-form); space-separated values land in the next positional slot` when the positional value is the literal `true` or `false` AND the command declares any boolean flag. The plain disagreement message stays unchanged for non-boolean cases. New helper `hasBoolFlag(cmdDef)` keeps the predicate close to the call site; regression tests cover both `true` and `false` variants alongside the non-boolean baseline.
- **Envelope handling extends to dependents / dependencies endpoints** (I27-AA verification): Conductor 17.10.0 wrapped the three remaining bare-array readers the iter-27 R1 sweep missed (`services_dependents`, `apps/:id/dependencies`, `services/:type/:id/dependencies`) in single-key envelopes (`{dependents: [...]}` / `{dependencies: [...]}`). The CLI's shape-keyed `unwrapArrayEnvelope` already handles them; new sub-test `iter-27 R2 dependents / dependencies envelopes unwrap` enumerates both keys against the helper to self-document the closure. No CLI binary change.
- **`-f /dev/stdin` reads alias the cached `-` sentinel** (I27-S / I27-X root cause): pre-fix, `runos apps env-vars set ... -f /dev/stdin <<< 'envVars: {}'` (and any kubectl-style invocation passing the literal `/dev/stdin` path instead of the `-` sentinel) drained stdin twice: first inside `bodyFileProvidesField` / `bodyFilePresentsField` from the missing-required pre-flight gate, then again inside `collectInput`. The `loadYAMLFile` stdin cache was keyed on the literal `-` only, so the `/dev/stdin` path bypassed it; the second read got 0 bytes (EOF), `yaml.Unmarshal` produced `nil`, and the wire body sent `null`. Conductor (correctly) returns a structured `400` for a real empty body (`{envVars: {}}` → "Refusing to clear ALL plain env vars on an empty payload..."); on a `null` body its internal-error path produced a bare `{error: "Internal server error", statusCode: 500}` which the test bench had attributed to conductor (I27-S env-collision + I27-X empty-payload). New `isStdinPath` helper aliases `-`, `/dev/stdin`, `/dev/fd/0`, and `/proc/self/fd/0` into the same `sync.Once`-cached branch so every stdin shape round-trips consistently across pre-flight and dispatch. Conductor needed no fix: both 400s render correctly when the CLI sends a non-`null` body. Regression tests `stdin alias /dev/stdin caches like dash sentinel (I27-S/X)` and `isStdinPath enumerates every stdin alias` pin both halves.
- **`runos deploy` validates the `dockerfile:` path against the resolved archive root before upload** (I27-Y pre-flight): pre-fix, a misconfigured `dockerfile:` field (typo, or a path relative to the yaml's own directory instead of `sourceDir` as the convention demands) silently uploaded the tarball, and the cluster-side build server failed late with `failed to read dockerfile: open <name>: no such file or directory` after the user had already burned the archive-upload round-trip. New `ResolveDockerfilePath(archiveRoot, dockerfile)` helper in `internal/deploy/config.go` (symmetric in shape with `ResolveArchiveRoot`) rejects absolute paths, paths that escape the archive root, missing files, and non-regular files (e.g. a `Dockerfile` directory). Wired into `cmd/deploy.go:runDeploy` right after `ResolveArchiveRoot` so the gate fires before `PrepareDeployment` and `CreateTarball`. Empty `dockerfile:` defaults to `Dockerfile` at the archive root (Docker-frontend default, preserved). Regression test `TestResolveDockerfilePath` covers default + monorepo `apps/api/Dockerfile` + missing + absolute + escapes + variant `Dockerfile.prod` + directory-shaped target.
- **Pulled VCS yaml round-trips `configPath`** (I27-M / I27-N): pre-fix, `runos apps pull` for VCS apps wrote `sourceDir` + `dockerfile` (round-tripped from the conductor 17.7.0 AppDocument reconciliation) but always omitted `configPath`. A fresh `git clone && runos apps pull` produced a yaml that the user couldn't tell where (in the source repo) the canonical yaml lives. New `PulledApp.ConfigPath` field with `yaml:"configPath,omitempty"`; `BuildPulledApp` reads `raw["configPath"]` so the round-trip now carries every build-metadata field the AppDocument stores. CLI-deploy apps (no stored configPath) still pull yamls without the key thanks to `omitempty`. Deploy already accepts `configPath:` as an explicit override in the local yaml (`cmd/deploy_vcs.go:resolveVcsConfigPath` honours it ahead of the git-rev-parse auto-derive), so the new field is wire-compatible end-to-end. Regression tests `TestBuildPulledApp_VcsBuildMetadataRoundTrip` (positive) + `TestBuildPulledApp_CliDeployOmitsConfigPath` (omitempty).
- **`runos services <db> exec-sql --read-write` warns when raw DDL bypasses conductor's user-list cache** (I27-AE residual): conductor 17.13.0 added per-verb cache-invalidation hooks to `grant-database` / `revoke-database` / `create-user` etc. so `services_<db>_users` stays in sync. Raw `exec-sql --read-write 'DROP ROLE foo'` still bypasses those hooks: the DDL hits the live database, succeeds, and the conductor's cached listing keeps the role around until something else triggers a refresh. The CLI can't refresh the cache from here, but the new post-dispatch hook in `internal/dynacmd/executor.go:warnIfRawDDLBypassedCache` detects destructive USER / ROLE / DATABASE DDL (DROP / CREATE / ALTER, regex-keyed for permissiveness) on a successful `services/<db>/{id}/exec-sql` and prints a one-line stderr warning naming the gap and the managed verbs that DO refresh the cache. Read-only queries (`readWrite: false`) and non-DDL queries pass silently. Regression test `TestQueryHasRawUserRoleDDL` covers 17 cases (positive + negative + comment-prefix + identifier-token-boundary).
- **`deploy.GetAppDependencies` accepts the conductor 17.7.0 `{dependencies: [...]}` envelope** (I27-AG): pre-fix, `runos deploy`'s laptop dependency check decoded `apps/:id/dependencies` directly into `[]deploy.AppDependency` and the conductor's envelope-everywhere migration (sibling of I27-AA / I27-T sweep) hit `cannot unmarshal object into Go value of type []deploy.AppDependency`, taking the dependency-resolution path offline (LOW: warning-only, doesn't block deploy outcomes, but pollutes the log stream). The existing `unwrapArrayEnvelopeDeploy` helper (already in use by `FindAppByName`) is now also threaded through `GetAppDependencies` so the legacy bare-array shape AND the new envelope shape both round-trip cleanly during the migration window. One-line wire-up, four-case regression test `TestGetAppDependencies_AcceptsEnvelope` covers envelope / legacy / empty-envelope / empty-bare-array.
- **Wire-shape regression test pins `dockerfile` field path-preservation on the deploy body** (I27-Y verification): I27-Y reports persistently flagged the CLI as the source of the build server's basename-only `--opt filename=Dockerfile` invocation. New `internal/deploy/wire_shape_i27y_test.go:TestPrepareDeployment_WireShape_DockerfilePath` spins up a fake conductor with `httptest.NewServer`, captures the exact bytes the CLI POSTs to `/prepare-cli-deployment` for a monorepo-shaped `DeployConfig{SourceDir: "../../..", Dockerfile: "apps/api/Dockerfile"}`, and asserts the wire body carries `dockerfile: "apps/api/Dockerfile"` (full path) AND drops `sourceDir` (`json:"-"`). End-to-end confirmation that the path stripping the build server observes happens downstream of the CLI, not in it. The earlier I27-Y CLI work (`ResolveDockerfilePath` pre-flight gate + `NginxDockerfileHint` advisory) covers the CLI's available surface.
- **`runos deploy` advises against bare `nginx:` base images for static sites** (I27-G CLI surface): companion to the `applications.md` "Static-site Dockerfile templates (non-root constraint)" docs section. A Dockerfile that pulls the upstream `nginx:<tag>` image (binds port 80, requires root) crashes the pod with `CrashLoopBackOff` at startup on every RunOS cluster because cluster policy refuses root containers. New `NginxDockerfileHint(dockerfilePath)` helper in `internal/deploy/config.go` scans the Dockerfile for `FROM nginx:<tag>` lines (case-insensitive, `--platform=` flag-tolerant, multi-stage-aware, word-boundary-checked so `mynginxfork` doesn't trip) and returns a one-line stderr hint pointing at the `nginxinc/nginx-unprivileged:<tag>` drop-in (listens on 8080 as non-root). Wired into `cmd/deploy.go:runDeploy` right after the I27-Y `ResolveDockerfilePath` pre-flight so the hint fires before any upload. Non-blocking: some users patch the upstream image themselves to drop root, so refusal would be too aggressive; an advisory is the right tradeoff. Regression test `TestNginxDockerfileHint` covers bare nginx + unprivileged-drop-in (negative) + multi-stage (intermediate-nginx triggers) + token-boundary (`mynginxfork` doesn't trigger) + missing file.

### Renames (breaking since v0.3.9)

- **Env var**: `CONDUCTOR_API_URL` → `RUNOS_API_URL`. Update CI secrets and shell rc files.
- **Config key**: `runos config set conductor-url ...` → `runos config set api-url ...`. Existing on-disk config files keep working unchanged (the internal JSON field is still `conductor_url`).
- **Status output label**: `Conductor:` → `API:` in `runos status`.
- **Topic prose / README**: every reference to the internal codename "conductor" replaced with "RunOS API" or equivalent. Internal Go field names and the `RemoteDomains.Conductor` CDN-config schema kept as-is.

### Internal

- `internal/apps/`, `internal/services/`: new packages backing the IaC commands.
- `internal/auth/resolve.go`: `ResolveToken(cfg)` is the single auth-token entry point.
- `internal/dynacmd/executor.go`: `ExecuteWithInput` programmatic entry; `*APIError` typed error; shared `dispatch` between Execute paths.
- `internal/manifest/types.go`: `Output.Fields` typed as `[]OutputField` with custom Unmarshal/Marshal for forward-compat.
- `internal/jobs/`: `EmitFollowDeltas(io.Writer, ...)` (replaced terminal-clear + spinner repaint), `EmitFollowLogs` for streamed work-item output.
- `internal/git/`: minimal wrapper around `git rev-parse` / `git status` for VCS-deploy SHA resolution.
- New direct deps: `github.com/pmezard/go-difflib` (unified-diff output), `golang.org/x/term` promoted from indirect to direct.

### Release-workflow

- `vX.Y.Z-<suffix>` tags are auto-marked as GitHub prereleases (so `releases/latest/...` keeps serving the previous stable). Stable tags (`vX.Y.Z` with no suffix) become the latest. The release workflow creates the release as a draft so asset uploads succeed under Immutable Releases, then publishes the draft in a follow-up step. Release notes are extracted from this CHANGELOG by section header.

## v0.3.9

### New

- **Empty-secret-env wipe gate**: `apps sync` now refuses to push when the local secret-env file is empty (or carries only platform-injected names like `DATABASE_URL`) AND the server has user-set secret keys. Catches the fresh-checkout footgun where a gitignored secret file isn't on disk and the LLM/CI silently wipes prod secrets. The refusal names the keys that would be deleted. Pass `--allow-empty-secret-env` (CLI) / `allow_empty_secret_env=true` (MCP) when the wipe is intentional. The `--yes` confirm-skip flag does NOT waive this gate, since `--yes` is "skip the prompt for the safe case" and not "I'm OK with destructive ops".
- **`apps pull --keep-env`**: opt-out from env file rewrites. `apps pull` writes both `.runos.<cid>.<id>.env` and `runos.<cid>.<id>.config.env` from server values by default; `--keep-env` preserves the local files instead. Useful when the user has dev-only env edits they want to keep while still refreshing yaml, secret-files, and overrides. Stdout shows the kept paths so the user knows nothing was rewritten and can run `apps diff` to see the divergence. Exposed on the MCP `apps_pull` tool as `keep_env`.
- **`jobs_follow` MCP tool**: long-poll streaming wrapper around `runos follow <jobId>`. Blocks until the job reaches a terminal state (success / failed / cancelled) or the 10-minute subprocess timeout fires, then returns the full streamed work-item log + final status as a single text payload. The canonical "wait until done" verb for orchestrations: cheaper than polling `jobs_show` in a loop and doesn't burn the LLM's prompt cache. Read-only.
- **TESTING.md**: testing philosophy, conventions, patterns, and a running list of regression tests by fix. The bar: a test earns its place if a future change that re-introduces a known bug would fail it. Pure-function regression tests on extracted helpers are the dominant pattern; we explicitly do NOT write cobra-level subprocess tests or end-to-end tests against a live conductor.

### Bug fixes

- **`runos deploy` env merge flipped to local-wins**: the pre-deploy sync used to refuse on any same-key-different-value conflict between local and server, blocking the dominant path (every version bump, every flag flip) until the user manually reconciled. The merge now copies the local file verbatim and adds only server-only keys; the deploy that follows pushes local up and replaces the server-side env in full. Same-key-different-value is silently the local value's win, which matches what the user expected when they edited the file.
- **`runos deploy` skips env file write when the merge is a no-op**: if the local file already has every server key, the merge skips the write entirely. Prevents the inverse footgun where a fresh checkout with no local env file would produce a strict no-op merge, the file would never materialise, and the subsequent deploy body would carry an empty `customEnvVars` map that conductor interpreted as "user wants empty ConfigMap" and wiped the server's env on every deploy.
- **`apps sync` override delete now removes the orphan local file**: the delete op carries `LocalLeaf` (the leaf filename inside `appDir/overrides/` that pull would have written for this override, derived via `OverrideFilenames` over the full server list so collision-disambiguated names match what pull wrote). Apply unlinks the local file alongside the server delete; previously deleting an override server-side left the local file orphaned and the next pull-then-sync would re-create it.
- **`apps diff` no longer flags local-only `sourceDir` as drift forever**: `sourceDir` is a CLI-only field (build-context resolution for `runos deploy`) that never reaches conductor and never appears in server-rendered yaml. The diff engine strips top-level `sourceDir:` lines from local bytes before comparison. Pulled yamls that include `sourceDir: ..` (the directory-per-app shape) no longer report drift on every diff.
- **`apps pull` preserves `resourceRequirementClassId: "custom"`**: prior versions rewrote the literal string `"custom"` as empty during pull, which fought with `apps show` (which surfaced `resourceRequirementClassId="custom"` from the server's `resolveRRC` synthesis). Pull now preserves the literal so `apps_show` and `apps_pull` return symmetric views; materialised cpu/memory fields are still emitted alongside, since `"custom"` by itself carries no values.
- **`apps sync` no longer renders preserve-sentinels as drift**: `clusterDomainId`, `resourceRequirementClassId`, and `replicas` are partial-update fields on the conductor's PATCH endpoint (omitted local value means "preserve server value", not "clear / set to zero"). The wire-body builder already omitted these correctly, but the dry-run plan was rendering alarming `replicas: 1 -> replicas: 0` lines for every yaml that omitted them, scaring users into refusing the (harmless) sync. Drift reporting now mirrors the wire body's omit logic.
- **`apps diff` exits 2 on drift**: structured exit code so CI can branch on "config drift detected" without parsing stdout. Exit 0 = in sync; exit 2 = drift; any other non-zero = real failure.
- **MCP `apps_diff` translates exit code 2 to success**: drift is the structured signal `apps_diff` returns, not a failure mode. The MCP wrapper now hands the drift report back to the LLM caller as a normal text payload instead of wrapping it in a red error block. The translation lives in a pure helper (`interpretAppsCommandResult`) tested directly with synthesized `*exec.ExitError`. CI users still see the non-zero exit at the CLI layer.
- **`services sync` class-only swap no longer flips class to "custom"**: pulling a service materialises the active class's cpu/memory baseline into the local yaml. Editing only the class line and syncing was sending the OLD baseline cpu/memory in the wire body; conductor's `resolveRRC` interpreted those as overrides against the NEW class baseline and flipped class to "custom". The PATCH body is now drift-only (`computeDriftPatch`): fields whose local value matches the server are omitted, so a class-only swap touches only the class field.
- **`apps pull` user-set secret env count no longer double-counts platform-injected keys**: `envVarCount` in the pull JSON summary used to sum plain + secret without filtering, which inflated the count and disagreed with what the `apps_env-vars` endpoint returned. Now `envVarCount` matches `apps_env-vars` keys exactly, and a separate `secretEnvVarCount` reflects user-set secret keys only (platform-injected keys claimed by `requires.<alias>.env` mappings are filtered out of the user-facing count).
- **MCP `domains_create` / `domains_update` `providerOptions` round-trips booleans**: the manifest-driven schema generator was emitting `additionalProperties: {type: string}` for `object` fields, which forced LLMs to send `proxy: "true"` (string) into a field that `domains_list` returned as `proxy: true` (bool). The conductor's `normalizeProviderOptions` was coercing back, but the schema mismatch was visibly confusing. The generator now drops the `additionalProperties` constraint specifically for `providerOptions` so booleans round-trip natively.

### Improvements

- **`apps sync` drops the "remove these dead-config keys" advisory**: previous versions emitted a "the platform injects these at runtime" note when the local secret-env file contained names claimed by `requires.<alias>.env` mappings (DATABASE_URL, REDIS_URL, etc.). The advice was wrong-headed: those keys are written to the local file by `apps pull` so it matches the K8s Secret, and the pre-deploy merge re-introduces them on every deploy. Removing them makes local LESS accurate, not more. The note and its formatter are gone; the underlying detection helper is retained as `FindServerInjectedEnvCollisions` and now used only to filter `runos deploy`'s "got merged back" warning so the user isn't pestered about keys that re-appear by design.
- **Documentation**: CLAUDE.md links to TESTING.md from the testing section. New tests added across `internal/apps/sync_test.go`, `internal/apps/pull_test.go`, `internal/apps/diff_test.go`, `internal/mcp/apps_test.go`, `cmd/deploy_test.go`, `internal/services/sync_test.go` covering each fix above as a regression test.

## v1.0.0-rc.6

Adds VCS deploys, splits env vars into Secret + ConfigMap, and brings live build-log streaming to `runos deploy --follow`. Install via `https://get.runos.com/cli.sh?release=v1.0.0-rc.6`.

### New

- **VCS deploys**: `runos deploy` now dispatches on the app's `deployType`. CLI-deploy apps still tarball the local source and upload (unchanged). VCS-deploy apps send `{sha, configPath}` only; the conductor and cluster agent pull source from the linked GitHub/GitLab integration at the SHA, build in-cluster, push, and roll out. New flags: `--app <id>` for CI mode (no yaml on disk), `--sha` (defaults to `git rev-parse HEAD`), `--allow-dirty` (waives the dirty-tree refusal). The two modes never silently intermingle: passing `--sha` / `--allow-dirty` against a CLI-deploy app is a hard error, and passing `--app` against a CLI-deploy app is rejected after a server-side `deployType` lookup.
- **`configPath:` is the source of truth for a VCS yaml's repo location**: `runos deploy` auto-derives the repo-relative path of the local yaml (`git rev-parse --show-toplevel` + `filepath.Rel`) and sends it on every VCS deploy. Conductor persists it to the AppDocument, so subsequent CI deploys (`runos deploy --app <id> --sha <sha>` without a checkout) inherit the persisted value. Monorepos with apps under per-app subdirectories (`apps/billing/runos.dev.yaml`) work without manual config. Explicit `configPath:` field in the yaml is still honoured as the escape hatch for non-standard layouts.
- **Env vars split into Secret + ConfigMap**: alongside the existing `.runos.<cid>.<id>.env` (sensitive, gitignored, K8s Secret), `runos deploy` / `apps pull` / `apps sync` / `apps diff` now also handle `runos.<cid>.<id>.config.env` (plain, committed, K8s ConfigMap). Both flow into the pod via `envFrom` so app code reads them identically. A key may not appear in both files at once; conductor refuses the deploy with a 400 listing conflicts, and the CLI pre-flights the same check. Pull/sync/diff round-trip both sides independently.
- **`runos deploy --follow` streams per-step build logs**: the follow poller now fetches new work-item log entries on each tick (cursor-paginated via the new `/jobs/:id/workitems/:id/logs` endpoint) and indents them under their parent step. Previously only status transitions surfaced; long-running build steps now show progress in real time. GitHub Actions runs wrap the streamed output in a `::group::` block so it collapses by default and surface the public URL via `::notice::` after a successful deploy.
- **`requires.<alias>.env` collision detection**: when a local env file hand-authors a key that the platform injects at runtime from a linked service's credentials (e.g. `DATABASE_URL` written locally while `requires.db.env.url=DATABASE_URL` claims it), `runos deploy` and `apps sync` flag it as informational dead config. Conductor drops the colliding key from `customEnvVars` on every deploy so the runtime value always reflects the linked service; the message points at the cosmetic cleanup.
- **Pre-deploy code drift gate**: `runos deploy` refuses when newer CLI archives exist on the server (someone else deployed via console or CI between this directory's last `apps pull --code` / deploy and now). The check is fail-closed (API failure refuses the deploy) so a fail-open never lets through the very thing the gate exists to prevent. Pass `--force` to override.
- **`runos deploy --force`**: a single bypass flag for both pre-deploy gates (yaml drift and code drift). The diff is shown either way.

### Improvements

- **`apps pull` / `apps diff` correctly read `deployType`**: prior versions conflated provider name with deploy type and stored `github-arc` / `gitlab-runner` as `deployType`, producing perpetual drift on `apps diff` because the server emits `vcs`. Provider identity now flows via the `Integration` block alongside repo/branch metadata; `deployType` carries the canonical `cli` | `vcs` value with `integrationType` as a legacy fallback for older conductors.
- **Class-flap heads-up note on `apps diff`**: when the server stored `resourceRequirementClassId=custom` because cpu/memory/replicas overrides disagree with the named class's defaults (server's `resolveRRC` synthesis), the diff now surfaces a one-line note explaining the mechanic and how to round-trip cleanly. Previously this looked like fresh class drift on every sync.
- **MCP `deploy` tool gains VCS parameters**: `app`, `sha`, `allow_dirty`. Pre-validation stays in the CLI binary (one source of truth for the deployType branching rule) so the MCP shim only translates args. The tool description spells out which flags belong with which deploy type.
- **MCP JSON Schema for object/array fields**: the manifest-driven tool generator now emits `additionalProperties: {type: string}` for `object` fields and `items: {type: string}` for `array` fields. Strict-validating LLM clients (some Anthropic SDK tool wrappers) previously rejected the unannotated schemas. The `object` manifest type now maps to JSON Schema `object` instead of being collapsed to `string`.
- **MCP defensive JSON-string coercion**: when an MCP client ignores the declared field type and sends an `object` / `array` field as a JSON-encoded string (`envVars: '{"K":"v"}'`), the executor now decodes in-place so the wire body matches the manifest's declared type. Conductor previously rejected it as "not an object".
- **`apps pull` pulls both env-var sides independently**: secret env file is gitignored on first pull (created with mode 0600), plain config env file is created with mode 0644 ready to commit. Diff/sync round-trip each side against its corresponding K8s resource.
- **Pre-deploy drift gate validates IDs as defence-in-depth**: app ID and cluster ID from the local yaml are charset-validated via `apps.ValidateIdentifier` before being joined into URLs, so a tampered yaml can't smuggle path components into API requests.
- **Build stamp on local builds**: `make local` now stamps `dev-<utc-timestamp>` into the version string, so dev binaries are version-distinguishable in the wild. Stable releases continue to stamp the git tag.
- **Documentation**: the README and CLAUDE.md gain a deploy-verb section covering the deployType dispatch, the three cluster-id sources (flag → config → yaml), the SHA / dirty-tree gate, and the env-var Secret/ConfigMap split.

### Bug fixes

- **`apps diff` `--redact-secrets` covers the new secret env section**: values in the new `SecretEnv` `SectionDiff` are redacted in the unified-diff output by the same flag that already redacts `.env` content. The MCP `apps_diff` tool keeps passing `--redact-secrets` so secret values never reach the LLM context.

## v1.0.0-rc.5

Bug fix on top of v1.0.0-rc.4. Install via `https://get.runos.com/cli.sh?release=v1.0.0-rc.5`.

### Bug fixes

- **`services pull` no longer projects class-derived cpu/memory/replica fields when a real resource class is stored**: now that the API manifest declares `replicas` / `cpuRequestMc` / `cpuLimitMc` / `memoryRequestMb` / `memoryLimitMb` as settable on `services/<type>/{id}/update` (so they can be overridden), the manifest-driven pull was projecting them into every yaml regardless of the stored class. With a real class set, the API encapsulates those values, so the yaml only needs the class. The CLI now mirrors the apps_pull preset-wins gate: real class → strip the five derived fields; `custom` (or empty / legacy) → keep them as the user-owned source of truth. Eliminates the false drift introduced by rc.4 on every service whose manifest exposes resource fields.

## v1.0.0-rc.4

CI/CD UX fixes on top of v1.0.0-rc.3. Install via `https://get.runos.com/cli.sh?release=v1.0.0-rc.4`.

### Improvements

- **Yaml is the source of truth for cluster id**: `apps sync` / `apps diff` / `apps pull <yaml>` / `apps pull` (auto-detect) / `apps list-previous-uploads` and `services sync` / `services diff` / `services pull <yaml>` now read the cluster id from the yaml's `cid:` field. `--cid` (and `RUNOS_CLUSTER_ID`) become optional cross-check guards: passed values are still validated against the yaml, and any mismatch refuses the operation. CI loops over committed yamls no longer need a default cluster set, no `--cid` per call, no `RUNOS_CLUSTER_ID` env var. Commands that don't have a local yaml to source from (`apps pull --all`, `apps pull --app-id`, `services pull --type+--service-id`) still require an explicit cluster id.
- **CDN config schema**: `RemoteDomains` now reads the canonical `api` field, falling back to the legacy `conductor` field for older payloads. Driven by a new `APIURL()` resolver, used everywhere the CDN environment is consumed.

## v1.0.0-rc.3

Build + release-workflow fixes on top of v1.0.0-rc.1. Install via `https://get.runos.com/cli.sh?release=v1.0.0-rc.3`. (rc.2 was an aborted attempt: the tag exists on GitHub but no release was published, and rc.3 supersedes it.)

### Bug fixes

- **Windows build**: `syscall.O_NOFOLLOW` (used in `apps_pull`'s symlink-refusal write path) doesn't exist on Windows, so the rc.1 release workflow failed at the `windows/amd64` step. Replaced with a build-tagged constant: protection is preserved on Unix; the flag is a no-op on Windows where the equivalent OS-level guarantee isn't available.
- **Release workflow under Immutable Releases**: the repo has Immutable Releases enabled, which disallows asset uploads after a release is published. The workflow now creates the release as a draft so asset uploads succeed, then publishes the draft in a follow-up step.

## v1.0.0-rc.1

Foundational release. Introduces IaC workflows for both apps and services, headless authentication for CI/CD, and a topic-driven documentation system for AI assistants. This is a release candidate for testing; install via `https://get.runos.com/cli.sh?release=v1.0.0-rc.1`. The default install URL keeps serving the previous stable.

### New

- **Apps as IaC**: `runos apps pull` / `apps diff` / `apps sync` round-trip an app's full configuration (yaml, env vars, secret files, manifest overrides) between cluster and disk. Pull supports bulk mode (`--all`), single-app mode, code-archive download (`--code` / `--code-version`), drift gates, and section-level diff reports. Sync pushes deltas via dedicated endpoints (PATCH /apps, env-vars, secret-files, overrides, requires).
- **Services as IaC**: `runos services pull` / `services diff` / `services sync` for postgresql, valkey, mysql, and any future service type the manifest exposes. Schema is manifest-driven (zero per-type code in the CLI); a new service type or new settable field on the API side flows through automatically after `runos manifest update`.
- **Service yaml shape is desired-state only**: `runos.service.<cid>.<type>.<sid>.yaml` contains exactly the fields the user can set (`add.Input.Fields ∪ update.Input.Fields ∪ add.Input.Flags`). Audit, derived, and operational fields the show response carries are filtered out automatically. Filename is convention; renames are tolerated via header-based lookup.
- **Apps integration with services IaC**: `apps_pull` cascades into pulling linked service yamls; `apps_sync` refuses `requires:` entries without an `id:` (provisioning is the service-yaml's job); `runos deploy` keeps the `requires.<alias>.class` shorthand and now writes a service yaml on first-time provisioning.
- **PAT-based authentication**: `runos account api-keys add` / `list` / `show` / `update` / `revoke` for managing personal access tokens. The CLI consumes a PAT via `RUNOS_API_KEY` (plus `RUNOS_ACCOUNT_ID`, optional `RUNOS_API_URL`); when set, the CLI bypasses Firebase entirely. Headless CI runners need no `~/.runos/config.json` and no interactive `runos login`.
- **Topic-driven MCP documentation**: the MCP read server now requires the LLM to consume a minimum number of distinct topics (apps, services, api-keys, cicd, ...) before non-topic tools unlock, so AI assistants ground themselves in the workflow before executing it. Topic content is delivered to MCP clients separately from the CLI binary.
- **Service delete guard (409)**: `DELETE /services/<type>/<id>` returns a structured 409 listing dependent apps when any reference the service. The CLI renders this as a multi-line refusal naming each dependent and its alias, with reconcile guidance.
- **`runos follow <jobId>`**: blocking job-status follower with line-oriented output (one line per state change, no escape codes, no spinners, no repaints). Suitable for CI logs and AI consumers; exit code gates downstream pipeline steps.

### Improvements

- **Pre-deploy drift gate**: `runos deploy` refuses when local diverges from server state (yaml or source archive). Pass `--force` to override; the diff is shown either way.
- **Manifest schema tolerance**: `output.fields` now accepts both legacy bare-string entries and the new rich-object shape (`{name, type, description, enum}`). New API-side field metadata flows through without a CLI release.
- **Tarball walker** unconditionally excludes RunOS-managed manifests (`runos*.yaml`, `runos.*/`, `overrides/`, dot-prefixed files) from every deploy archive, regardless of `.dockerignore`. Cross-cluster config can't bleed into a source upload.
- **`apps_pull` writes a default `.dockerignore`** on first pull (skipped when present) covering the same exclusions so external Docker builders honour the same rules.
- **Multi-cluster project shape**: directory-per-app (`runos.<cid>.<id>/` subdirs with `sourceDir: ".."`) is the recommended shape for repos deploying to multiple clusters; `apps_pull` defaults to it and stamps `sourceDir` automatically.

### Renames (breaking since v0.3.9)

- **Env var**: `CONDUCTOR_API_URL` → `RUNOS_API_URL`. Update CI secrets and shell rc files.
- **Config key**: `runos config set conductor-url ...` → `runos config set api-url ...`. Existing on-disk config files keep working unchanged (the internal JSON field is still `conductor_url`).
- **Status output label**: `Conductor:` → `API:` in `runos status`.
- **Topic prose / README**: every reference to the internal codename "conductor" replaced with "RunOS API" or equivalent. Internal Go field names and the `RemoteDomains.Conductor` CDN-config schema kept as-is.

### Internal

- `internal/apps/`: new package backing the apps IaC commands.
- `internal/services/`: new package backing the services IaC commands.
- `internal/auth/resolve.go`: `ResolveToken(cfg)` is the single auth-token entry point; used by every API-touching subcommand.
- `internal/dynacmd/executor.go`: new `ExecuteWithInput` programmatic entry; `*APIError` typed error so callers can branch on status; shared `dispatch` between Execute paths.
- `internal/manifest/types.go`: `Output.Fields` typed as `[]OutputField` with custom Unmarshal/Marshal for forward-compat.
- `internal/jobs/`: `DisplayFollow` (terminal-clear + spinner repaint) replaced by `EmitFollowDeltas(io.Writer, ...)`.
- New direct dep: `github.com/pmezard/go-difflib` (unified-diff output).
- `golang.org/x/term` promoted from indirect to direct.

### Release-workflow

- `vX.Y.Z-<suffix>` tags are now auto-marked as GitHub prereleases (so `releases/latest/...` keeps serving the previous stable). Stable tags (`vX.Y.Z` with no suffix) become the latest. See [.github/workflows/release.yml](.github/workflows/release.yml).

## v0.3.9

### Improvements
- **Fix changelog extraction in release workflow** — release notes were not being extracted due to a broken awk/sed command
- **Add CI test for changelog extraction** — verifies all changelog entries are extractable on every push/PR
- **Document release process** in README and CLAUDE.md

## v0.3.8

### Improvements
- **Changelog-driven release notes** — GitHub releases now pull notes from CHANGELOG.md, falling back to auto-generated notes if no entry exists

## v0.3.7

### Bug fixes
- **Fix MCP path parameter substitution** — AI models sending prefixed field names (e.g. `app_id` instead of `id`) now resolve correctly by deriving the expected prefix from the URL path entity
- **Fix boolean type mapping in MCP tool schema** — boolean manifest fields (like `enabled` on overrides) were incorrectly exposed as `string`, causing API rejections

### Improvements
- **MCP CLI version check** — the `cli_version-check` tool now auto-injects the current CLI version and OS, allowing AI assistants to check for updates without needing to know the version

## v0.3.6

### Bug fixes
- **Fix MCP path parameter substitution when AI prefixes field names** — resolves issue where literal `:id` appeared in API URLs instead of actual resource IDs
